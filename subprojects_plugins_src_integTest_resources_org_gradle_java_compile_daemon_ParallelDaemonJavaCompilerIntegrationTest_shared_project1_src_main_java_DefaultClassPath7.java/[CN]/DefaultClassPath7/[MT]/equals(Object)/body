{
  if (obj == this) {
    return true;
  }
  if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  DefaultClassPath7 other=(DefaultClassPath7)obj;
  return files.equals(other.files);
}
