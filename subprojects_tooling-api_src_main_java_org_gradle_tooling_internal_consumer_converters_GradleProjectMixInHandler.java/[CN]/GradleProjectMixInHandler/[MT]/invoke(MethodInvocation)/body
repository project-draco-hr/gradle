{
  if (invocation.getName().equals("getGradleProject") && invocation.getDelegate() instanceof EclipseProjectVersion3) {
    invocation.setResult(new GradleProjectConverter().convert((EclipseProjectVersion3)invocation.getDelegate()));
  }
}
