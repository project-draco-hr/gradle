{
  if (!isValidXmlName(name)) {
    throw new IllegalArgumentException("Invalid attribute name: " + name);
  }
  write(" ");
  write(name);
  write("=\"");
  writeEncoded(value);
  write("\"");
  return this;
}
