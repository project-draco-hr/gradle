{
  if (!isValidXmlName(name)) {
    throw new IllegalArgumentException(String.format("Invalid element name: '%s'",name));
  }
  maybeFinishElement();
  startElement=true;
  elements.add(name);
  write("<");
  write(name);
  return this;
}
