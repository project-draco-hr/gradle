{
  if (this == o) {
    return true;
  }
  if (!getClass().equals(o.getClass())) {
    return false;
  }
  WtpComponent wtp=(WtpComponent)o;
  return Objects.equal(deployName,wtp.deployName) && Objects.equal(contextPath,wtp.contextPath) && Objects.equal(wbModuleEntries,wtp.wbModuleEntries);
}
