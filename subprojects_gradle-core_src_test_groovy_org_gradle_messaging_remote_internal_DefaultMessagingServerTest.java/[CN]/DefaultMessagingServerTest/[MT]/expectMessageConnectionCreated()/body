{
  final MultiChannelConnection<Message> messageConnection=context.mock(MultiChannelConnection.class,String.valueOf(counter++));
  context.checking(new Expectations(){
{
      one(connector).listen();
      will(returnValue(messageConnection));
    }
  }
);
  return messageConnection;
}
