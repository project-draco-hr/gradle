{
  context.checking(new Expectations(){
{
      one(dependencyDescriptorFactoryMock).createDescriptor(getParentModuleDescriptor(),TEST_DESCRIPTOR,moduleDependency.isForce(),true,false,TEST_CONF_SET,moduleDependency.getExcludeRules().getRules());
      will(returnValue(expectedDependencyDescriptor));
    }
  }
);
  assertSame(expectedDependencyDescriptor,moduleDependency.createDepencencyDescriptor(getParentModuleDescriptor()));
}
