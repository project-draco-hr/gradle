{
  URI scriptUri=resolver.resolveUri(script);
  ScriptPlugin configurer=configurerFactory.create(new StrictScriptSource(new UriScriptSource("script",scriptUri)));
  for (  Object target : targets) {
    configurer.use(target);
  }
}
