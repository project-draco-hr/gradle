{
  File scriptFile=resolver.resolve(script);
  ScriptPlugin configurer=configurerFactory.create(new StrictScriptSource(new FileScriptSource("script",scriptFile)));
  for (  Object target : targets) {
    configurer.use(target);
  }
}
