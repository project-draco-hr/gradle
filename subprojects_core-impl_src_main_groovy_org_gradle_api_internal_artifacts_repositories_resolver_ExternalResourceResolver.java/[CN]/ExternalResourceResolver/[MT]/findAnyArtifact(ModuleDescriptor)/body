{
  for (  Artifact artifact : md.getAllArtifacts()) {
    ResolvedArtifact artifactRef=getArtifactRef(artifact,false);
    if (artifactRef != null) {
      return artifactRef;
    }
  }
  return null;
}
