{
  return new ResourceMDParser(){
    public ModuleVersionMetaData parse(    ExternalResource resource,    Artifact artifact){
      return ExternalResourceResolver.this.parse(artifact,resource);
    }
  }
;
}
