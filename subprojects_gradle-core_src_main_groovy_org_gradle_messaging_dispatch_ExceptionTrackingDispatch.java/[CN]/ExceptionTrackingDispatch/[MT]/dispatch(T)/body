{
  try {
    dispatch.dispatch(message);
  }
 catch (  Throwable t) {
    if (failure != null) {
      logger.error(String.format("Failed to dispatch message %s.",message),t);
    }
 else {
      failure=t;
    }
  }
}
