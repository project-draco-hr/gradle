{
  super.start();
  if (target.equals(SYSTEM_OUT)) {
    setWriter(createWriter(StandardOutputLogging.DEFAULT_OUT));
  }
 else {
    setWriter(createWriter(StandardOutputLogging.DEFAULT_OUT));
  }
}
