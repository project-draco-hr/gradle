{
  return new NamedDomainObjectFactory<T>(){
    @Override public T create(    String name){
      return BaseLanguageSourceSet.create(sourceSetImplementation,name,parentName,fileResolver,instantiator);
    }
  }
;
}
