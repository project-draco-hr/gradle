{
  final Compiler<JavaCompileSpec> compiler=getCompiler(inputs,sourceDirs);
  return new IncrementalCompilationFinalizer(compiler,analyzer,dependencyInfoSerializer,jarSnapshotFeeder,new ClasspathJarFinder(fileOperations),fileOperations);
}
