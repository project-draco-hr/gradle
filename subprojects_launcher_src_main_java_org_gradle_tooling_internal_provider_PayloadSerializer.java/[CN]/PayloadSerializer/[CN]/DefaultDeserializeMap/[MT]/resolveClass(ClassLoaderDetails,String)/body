{
  Class<?> aClass=null;
  if (overrides != null) {
    aClass=overrides.resolveClass(details,className);
  }
  if (aClass != null) {
    return aClass;
  }
  return map.resolveClass(details,className);
}
