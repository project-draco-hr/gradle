{
  try {
    File tempDir=File.createTempFile(prefix,"dir");
    tempDir.delete();
    tempDir.mkdirs();
    tempDir.deleteOnExit();
    return tempDir;
  }
 catch (  IOException e) {
    throw new UncheckedIOException(e);
  }
}
