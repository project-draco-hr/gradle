{
  context.checking(new Expectations(){
{
      one(fileMock).exists();
      will(returnValue(false));
      one(fileMock).getAbsolutePath();
      will(returnValue(fileMockAbsolutePath));
    }
  }
);
  digesterUtil.digestDirectory(digesterMock,fileMock,okDirectorySize);
}
