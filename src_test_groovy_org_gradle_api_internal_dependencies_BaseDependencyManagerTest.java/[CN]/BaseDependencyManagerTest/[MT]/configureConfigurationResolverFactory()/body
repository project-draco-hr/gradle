{
  context.checking(new Expectations(){
{
      allowing(configurationResolverFactoryMock).createConfigurationResolver(testConfiguration,dependencyContainerMock,dependencyResolversMock,artifactContainerMock,configurationContainerMock);
      will(returnValue(testConfigurationResolver));
    }
  }
);
}
