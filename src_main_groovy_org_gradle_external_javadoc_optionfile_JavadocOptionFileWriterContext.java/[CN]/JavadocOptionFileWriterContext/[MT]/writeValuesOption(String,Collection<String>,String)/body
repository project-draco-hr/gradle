{
  writeOptionHeader(option);
  write("\'");
  final Iterator<String> valuesIt=values.iterator();
  while (valuesIt.hasNext()) {
    write(valuesIt.next());
    if (valuesIt.hasNext()) {
      write(joinValuesBy);
    }
  }
  write("\'");
  newLine();
  return this;
}
