{
  Object value=destDir;
  while (true) {
    if (value instanceof Closure) {
      Closure closure=(Closure)value;
      value=closure.call();
    }
 else {
      break;
    }
  }
  return value.toString();
}
