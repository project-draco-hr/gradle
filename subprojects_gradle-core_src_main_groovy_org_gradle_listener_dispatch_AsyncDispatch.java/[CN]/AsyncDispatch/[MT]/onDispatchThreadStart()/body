{
  lock.lock();
  try {
    if (state != State.Init) {
      throw new IllegalStateException("This dispatch has already been started.");
    }
    setState(State.Dispatching);
  }
  finally {
    lock.unlock();
  }
}
