{
  if (finished) {
    context.stopped();
    return;
  }
  if (!outgoingStopped) {
    context.dispatchOutgoing(new EndOfStreamEvent());
    outgoingStopped=true;
  }
  context.stopLater();
  stopping=true;
}
