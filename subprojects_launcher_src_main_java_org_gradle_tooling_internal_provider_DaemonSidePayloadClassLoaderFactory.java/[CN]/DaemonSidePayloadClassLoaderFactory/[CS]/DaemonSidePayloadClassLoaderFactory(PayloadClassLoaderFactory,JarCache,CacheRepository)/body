{
  this.delegate=delegate;
  this.jarCache=jarCache;
  cache=cacheRepository.cache("jars-1").withDisplayName("jars").withCrossVersionCache().withLockOptions(mode(FileLockManager.LockMode.None)).open();
}
