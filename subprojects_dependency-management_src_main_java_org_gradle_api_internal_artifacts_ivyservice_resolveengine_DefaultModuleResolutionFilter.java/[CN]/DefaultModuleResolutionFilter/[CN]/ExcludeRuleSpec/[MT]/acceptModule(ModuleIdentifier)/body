{
  return !(matches(moduleId.getGroup(),module.getGroup()) && matches(moduleId.getName(),module.getName()) && isWildcard(ivyArtifactName.getName())&& isWildcard(ivyArtifactName.getType())&& isWildcard(ivyArtifactName.getExtension()));
}
