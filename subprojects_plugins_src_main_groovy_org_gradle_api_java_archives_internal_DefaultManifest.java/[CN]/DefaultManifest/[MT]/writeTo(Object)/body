{
  IoActions.writeFile(fileResolver.resolve(path),new ErroringAction<Writer>(){
    @Override public void doExecute(    Writer writer) throws Exception {
      writeTo(writer);
    }
  }
);
  return this;
}
