{
  lock.lock();
  try {
    while (!endOfStreamReached) {
      try {
        condition.await();
      }
 catch (      InterruptedException e) {
        throw new GradleException(e);
      }
    }
  }
  finally {
    lock.unlock();
  }
}
