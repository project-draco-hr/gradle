{
  usingBuildFile(testFile("build.gradle").write("new DefaultTask()")).withTasks("tasks").runWithFailure().assertHasCause("Task of type 'org.gradle.api.DefaultTask' has been instantiated directly which is not supported");
}
