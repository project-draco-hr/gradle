{
  lock.lock();
  try {
    try {
      execResult.rethrowFailure().assertNormalExitValue();
    }
 catch (    ExecException e) {
      processFailure=e;
    }
    running=false;
    condition.signalAll();
  }
  finally {
    lock.unlock();
  }
}
