{
  if (imported && system) {
    systemImports.add(value);
  }
 else   if (imported) {
    imports.add(value);
  }
 else   if (system) {
    systemIncludes.add(value);
  }
 else {
    includes.add(value);
  }
}
