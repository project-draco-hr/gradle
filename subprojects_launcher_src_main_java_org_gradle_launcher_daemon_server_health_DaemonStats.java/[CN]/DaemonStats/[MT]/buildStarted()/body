{
  ++buildCount;
  currentBuildStart=timeProvider.getCurrentTime();
  if (buildCount == 1) {
    return format("Starting build in new daemon [memory: %s]",NumberUtil.formatBytes(maxMemory));
  }
 else {
    return format("Executing %s build in daemon [uptime: %s, performance: %s%%, memory: %s%% of %s]",NumberUtil.ordinal(buildCount),totalTime.getTime(),performance(allBuildsTime,gcStats),NumberUtil.percentOf(comittedMemory,maxMemory),NumberUtil.formatBytes(maxMemory));
  }
}
