{
  return taskMutator.mutate("TaskOutputs.includeDir(Object)",new Callable<TaskFileOutputPropertySpec>(){
    @Override public TaskFileOutputPropertySpec call() throws Exception {
      return addSpec(path);
    }
  }
);
}
