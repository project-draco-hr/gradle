{
  return taskMutator.mutate("TaskOutputs.includeFile(Object)",new Callable<TaskOutputFilePropertySpec>(){
    @Override public TaskOutputFilePropertySpec call() throws Exception {
      return addSpec(path);
    }
  }
);
}
