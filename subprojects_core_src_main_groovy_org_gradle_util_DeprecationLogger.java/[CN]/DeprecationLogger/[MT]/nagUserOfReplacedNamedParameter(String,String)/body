{
  if (NAMED_PARAMETERS.add(parameterName)) {
    LOGGER.warn(String.format("The %s named parameter is deprecated and will be removed in the next version of Gradle. You should use the %s named parameter instead.",parameterName,replacement));
  }
}
