{
  if (isEnabled() && PROPERTIES.add(propertyName)) {
    LOGGER.warn(String.format("The %s property %s. Please use the %s property instead.",propertyName,DEPRECATION_MESSAGE,replacement));
    logTraceIfNecessary();
  }
}
