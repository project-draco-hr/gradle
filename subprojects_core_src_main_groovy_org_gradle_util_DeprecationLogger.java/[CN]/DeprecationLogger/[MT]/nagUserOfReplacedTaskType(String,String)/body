{
  if (isEnabled() && TASKS.add(taskName)) {
    LOGGER.warn(String.format("The %s task type has been deprecated and will be removed in the next version of Gradle. Please use the %s instead.",taskName,replacement));
    logTraceIfNecessary();
  }
}
