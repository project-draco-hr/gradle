{
  if (isEnabled() && TASKS.add(taskName)) {
    LOGGER.warn(String.format("The %s task type %s. Please use the %s instead.",taskName,DEPRECATION_MESSAGE,replacement));
    logTraceIfNecessary();
  }
}
