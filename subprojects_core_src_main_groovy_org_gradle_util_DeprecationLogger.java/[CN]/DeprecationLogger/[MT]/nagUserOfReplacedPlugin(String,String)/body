{
  if (isEnabled() && PLUGINS.add(pluginName)) {
    LOGGER.warn(String.format("The %s plugin %S. Please use the %s plugin instead.",pluginName,DEPRECATION_MESSAGE,replacement));
    logTraceIfNecessary();
  }
}
