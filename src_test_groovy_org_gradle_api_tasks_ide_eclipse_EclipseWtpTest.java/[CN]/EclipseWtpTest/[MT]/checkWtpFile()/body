{
  File wtp=new File(getProject().getProjectDir(),EclipseWtp.WTP_FILE_DIR + "/" + EclipseWtp.WTP_FILE_NAME);
  assertTrue(wtp.isFile());
  assertThat(GFileUtils.readFileToString(wtp),Matchers.equalTo(IOUtils.toString(this.getClass().getResourceAsStream("expectedWtpFile.txt"))));
}
