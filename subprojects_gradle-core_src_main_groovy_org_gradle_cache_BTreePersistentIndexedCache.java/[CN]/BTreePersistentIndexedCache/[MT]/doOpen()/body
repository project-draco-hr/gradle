{
  header=new HeaderBlock();
  if (file.length() == 0) {
    nextBlock=header.getActualSize();
    newRootBlock();
    flush();
    backingCache.update();
  }
 else {
    nextBlock=file.length();
  }
  header.read();
}
