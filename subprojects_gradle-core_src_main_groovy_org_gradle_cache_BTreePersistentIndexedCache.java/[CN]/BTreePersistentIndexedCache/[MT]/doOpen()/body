{
  file=new RandomAccessFile(cacheFile,"rw");
  header=new HeaderBlock();
  if (file.length() == 0) {
    header.init();
    flush();
    backingCache.update();
  }
 else {
    nextBlock=file.length();
  }
  header.read();
}
