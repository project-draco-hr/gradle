{
  configurationProgressFormatter=new SimpleProgressFormatter(totalProjects,"projects");
  configurationProgress=progressLoggerFactory.newOperation(BuildProgressLogger.class).start("Configure projects",configurationProgressFormatter.getProgress());
}
