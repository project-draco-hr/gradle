{
  if (configurationProgress != null) {
    ProgressLogger logger=progressLoggerFactory.newOperation(BuildProgressLogger.class).start("Configuring project " + projectPath,projectPath.equals(":") ? "root project" : projectPath);
    projectConfigurationProgress.put(projectPath,logger);
  }
}
