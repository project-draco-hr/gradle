{
  if (!stopRequested.getAndSet(true)) {
    workQueue.dispatch(new Runnable(){
      public void run(){
        for (        Stage stage : stack) {
          stage.requestStop();
        }
      }
    }
);
  }
}
