{
  stopPending=false;
  if (!stopped) {
    stopped=true;
    protocolsStopped.countDown();
    contextQueue.add(new Runnable(){
      public void run(){
        outgoing.requestStop();
      }
    }
);
  }
}
