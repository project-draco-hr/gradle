{
  for (  PlatformParser parser : factories.keySet()) {
    if (parser.parse(name)) {
      return factories.get(parser).create(name);
    }
  }
  throw new InvalidUserDataException(String.format("Cannot create %s because there are no platforms that can parse it. Available platforms: %s",name,factories.keySet()));
}
