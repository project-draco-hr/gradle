{
  URI result=sourceUri;
  if (result == null) {
synchronized (this) {
      result=sourceUri;
      if (result == null) {
        sourceUri=result=sourceUriFactory.create();
      }
    }
  }
  return result;
}
