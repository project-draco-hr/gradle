{
  lock.lock();
  try {
    contendedActions.remove(target);
    if (contendedActions.isEmpty()) {
      communicator.stop();
      executor.requestStop();
    }
  }
  finally {
    lock.unlock();
  }
}
