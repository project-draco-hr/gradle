{
  DefaultSettings settings=new DefaultSettings(dependencyManagerFactory,projectDescriptorRegistry,buildSourceBuilder,settingsDir,gradleProperties,startParameter);
  settings.setSettingsScript(settingsScript);
  return settings;
}
