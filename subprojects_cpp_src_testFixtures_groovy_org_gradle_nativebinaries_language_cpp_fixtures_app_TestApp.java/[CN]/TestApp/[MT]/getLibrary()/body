{
  return new TestComponent(){
    @Override public List<SourceFile> getSourceFiles(){
      return getLibrarySources();
    }
    @Override public List<SourceFile> getHeaderFiles(){
      return Arrays.asList(getLibraryHeader());
    }
  }
;
}
