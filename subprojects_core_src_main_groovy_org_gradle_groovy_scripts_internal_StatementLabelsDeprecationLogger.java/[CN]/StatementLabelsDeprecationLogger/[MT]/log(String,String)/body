{
  DeprecationLogger.nagUserOfDeprecated("Usage of statement labels in build scripts",String.format("In case you tried to configure a property named '%s', replace ':' with '=' or ' '. Otherwise it will not have the desired effect. \n\n%s",label,sample));
}
