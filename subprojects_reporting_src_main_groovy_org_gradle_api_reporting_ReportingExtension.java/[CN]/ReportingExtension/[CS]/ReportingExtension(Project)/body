{
  this.project=(ProjectInternal)project;
  this.baseDir=new Callable<File>(){
    public File call() throws Exception {
      return ReportingExtension.this.project.getFileResolver().withBaseDir(ReportingExtension.this.project.getBuildDir()).resolve(DEFAULT_REPORTS_DIR_NAME);
    }
  }
;
}
