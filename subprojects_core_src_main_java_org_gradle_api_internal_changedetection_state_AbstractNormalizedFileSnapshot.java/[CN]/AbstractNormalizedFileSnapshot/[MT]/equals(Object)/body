{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  AbstractNormalizedFileSnapshot that=(AbstractNormalizedFileSnapshot)o;
  return Objects.equal(getNormalizedPath(),that.getNormalizedPath()) && Objects.equal(snapshot,that.snapshot);
}
