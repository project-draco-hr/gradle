{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  AbstractNormalizedFileSnapshot that=(AbstractNormalizedFileSnapshot)o;
  return snapshot.equals(that.snapshot) && getNormalizedPath().equals(that.getNormalizedPath());
}
