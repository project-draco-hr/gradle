{
  if (status.isPermGenSpaceExhausted()) {
    return new DaemonExpirationResult(GRACEFUL_EXPIRE,"because JVM perm gen space was exhausted");
  }
 else {
    return DaemonExpirationResult.NOT_TRIGGERED;
  }
}
