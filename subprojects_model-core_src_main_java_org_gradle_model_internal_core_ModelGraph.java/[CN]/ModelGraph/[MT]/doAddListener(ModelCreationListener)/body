{
  notifying=true;
  try {
    for (    ModelNode node : flattened.values()) {
      if (maybeNotify(listener,node)) {
        return;
      }
    }
    listeners.add(listener);
  }
  finally {
    notifying=false;
  }
}
