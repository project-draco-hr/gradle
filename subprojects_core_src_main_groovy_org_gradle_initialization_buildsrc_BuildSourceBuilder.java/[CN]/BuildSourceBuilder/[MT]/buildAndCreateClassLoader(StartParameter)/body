{
  ClassPath classpath=createBuildSourceClasspath(startParameter);
  ClassLoaderScope childScope=classLoaderScope.createChild("buildSrc");
  childScope.export(classpath);
  childScope.lock();
  return childScope;
}
