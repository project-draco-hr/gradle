{
  File file1=new File("a");
  File file2=new File("b");
  FileCollection collection=new DefaultFileCollection(file1,file2);
  assertThat(collection.getFiles(),equalTo(toLinkedSet(file1.getCanonicalFile(),file2.getCanonicalFile())));
}
