{
  onReceiveThreadStart();
  executor.execute(new Runnable(){
    public void run(){
      try {
        receiveMessages(receiver);
      }
  finally {
        onReceiveThreadExit();
      }
    }
  }
);
}
