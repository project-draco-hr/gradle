{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Key key=(Key)o;
  if (!portalUrl.equals(key.portalUrl)) {
    return false;
  }
  if (!value.equals(key.value)) {
    return false;
  }
  return true;
}
