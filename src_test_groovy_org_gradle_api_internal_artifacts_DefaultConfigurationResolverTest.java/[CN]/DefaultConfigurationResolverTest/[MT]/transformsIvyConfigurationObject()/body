{
  final Transformer<org.apache.ivy.core.module.descriptor.Configuration> testTransformer=context.mock(Transformer.class);
  context.checking(new Expectations(){
{
      one(configurationMock).addIvyTransformer(testTransformer);
    }
  }
);
  configurationResolver.addIvyTransformer(testTransformer);
}
