{
  if (currentTest != test)   throw new IllegalStateException("currentTest(" + currentTest + ") != test("+ test+ ")!");
  classProcessResult.addMethodResult(new TestMethodProcessResult(test.toString(),currentTestState,currentTestException));
  currentTest=null;
  currentTestException=null;
}
