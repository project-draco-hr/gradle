{
  while (true) {
    try {
      int c=System.in.read();
      if (c == -1 || c == 4) {
        logger.info("received end of stream (ctrl+d)");
        return;
      }
    }
 catch (    IOException e) {
      logger.error("Error reading input",e);
    }
  }
}
