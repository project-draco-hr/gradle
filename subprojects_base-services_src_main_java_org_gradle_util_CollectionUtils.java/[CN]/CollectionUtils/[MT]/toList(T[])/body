{
  if (things == null || things.length == 0) {
    return new ArrayList<T>(0);
  }
  List<T> list=new ArrayList<T>(things.length);
  for (  T thing : things) {
    list.add(thing);
  }
  return list;
}
