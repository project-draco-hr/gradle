{
  if (things == null) {
    return new HashSet<T>(0);
  }
  if (things instanceof Set) {
    return (Set<T>)things;
  }
  Set<T> set=new LinkedHashSet<T>();
  for (  T thing : things) {
    set.add(thing);
  }
  return set;
}
