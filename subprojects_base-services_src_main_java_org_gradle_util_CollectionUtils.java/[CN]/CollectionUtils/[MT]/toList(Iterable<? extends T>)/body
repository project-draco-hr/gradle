{
  if (things == null) {
    return new ArrayList<T>(0);
  }
  if (things instanceof List) {
    return (List<T>)things;
  }
  List<T> list=new ArrayList<T>();
  for (  T thing : things) {
    list.add(thing);
  }
  return list;
}
