{
  if (reasonKey == null) {
    throw new IllegalArgumentException("reasonKey can't be null!");
  }
  if (!reasonKeys.contains(reasonKey)) {
    reasonKeys.add(reasonKey);
  }
  if (reasonData != null) {
    this.reasonData.put(reasonKey,reasonData);
  }
}
