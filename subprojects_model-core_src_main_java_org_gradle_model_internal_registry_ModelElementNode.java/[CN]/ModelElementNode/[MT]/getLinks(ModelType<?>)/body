{
  return Iterables.filter(links.values(),new Predicate<ModelNodeInternal>(){
    @Override public boolean apply(    ModelNodeInternal link){
      link.ensureAtLeast(Discovered);
      return link.getPromise().canBeViewedAsMutable(type);
    }
  }
);
}
