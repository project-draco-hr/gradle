{
  renderers.put(type,new ReportRenderer<BinarySpec,TextReportBuilder>(){
    @Override public void render(    BinarySpec model,    TextReportBuilder output) throws IOException {
      renderer.render(type.cast(model),output);
    }
  }
);
}
