{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ModelPath modelPath=(ModelPath)o;
  if (!path.equals(modelPath.path)) {
    return false;
  }
  return true;
}
