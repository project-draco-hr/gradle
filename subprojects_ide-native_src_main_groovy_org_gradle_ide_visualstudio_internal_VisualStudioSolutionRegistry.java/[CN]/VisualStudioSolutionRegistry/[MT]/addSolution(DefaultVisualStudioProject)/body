{
  for (  DefaultVisualStudioSolution solution : this) {
    if (solution.getRootProject() == visualStudioProject) {
      return;
    }
  }
  DefaultVisualStudioSolution solution=getInstantiator().newInstance(DefaultVisualStudioSolution.class,visualStudioProject,fileResolver,projectResolver,getInstantiator());
  add(solution);
}
