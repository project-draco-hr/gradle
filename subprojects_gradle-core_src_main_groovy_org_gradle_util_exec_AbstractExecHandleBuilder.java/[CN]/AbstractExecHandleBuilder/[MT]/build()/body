{
  if (StringUtils.isEmpty(getExecutable())) {
    throw new IllegalStateException("execCommand == null!");
  }
  return new DefaultExecHandle(getWorkingDir(),getExecutable(),getAllArguments(),normalTerminationExitCode,getActualEnvironment(),standardOutput,errorOutput,input,listeners);
}
