{
  List<File> files=resolve();
  if (files.size() != 1) {
    throw new IllegalStateException(String.format("Configuration '%s' does not resolve to a single file.",getName()));
  }
  return files.get(0);
}
