{
  lock.lock();
  try {
    LOGGER.info("Stop requested. The daemon is running a build: " + running);
    stopped=true;
    activityListener.onStop();
    condition.signalAll();
  }
  finally {
    lock.unlock();
  }
}
