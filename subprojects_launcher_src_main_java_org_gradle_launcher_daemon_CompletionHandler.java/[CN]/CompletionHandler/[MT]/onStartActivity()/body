{
  lock.lock();
  try {
    if (running) {
      throw new BusyException();
    }
    running=true;
    activityListener.onStartActivity();
    condition.signalAll();
  }
  finally {
    lock.unlock();
  }
}
