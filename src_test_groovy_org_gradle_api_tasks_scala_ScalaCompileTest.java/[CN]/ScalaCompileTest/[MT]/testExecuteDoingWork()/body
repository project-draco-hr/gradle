{
  setUpMocksAndAttributes(scalaCompile);
  context.checking(new Expectations(){
{
      one(antScalaCompileMock).execute(scalaCompile.getScalaSrcDirs(),scalaCompile.getScalaIncludes(),scalaCompile.getScalaExcludes(),scalaCompile.getDestinationDir(),scalaCompile.getClasspath(),scalaCompile.getScalaCompileOptions());
    }
  }
);
  scalaCompile.execute();
}
