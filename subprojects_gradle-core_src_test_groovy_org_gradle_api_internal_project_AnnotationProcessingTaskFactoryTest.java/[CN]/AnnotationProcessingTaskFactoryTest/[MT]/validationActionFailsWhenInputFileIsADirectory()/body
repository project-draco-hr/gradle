{
  TaskWithInputFile task=expectTaskCreated(new TaskWithInputFile(existingDir));
  assertValidationFails(task,String.format("File '%s' specified for property 'inputFile' is not a file.",task.inputFile));
}
