{
  TaskWithOutputDir task=expectTaskCreated(TaskWithOutputDir.class,existingFile);
  assertValidationFails(task,String.format("Cannot create directory '%s' specified for property 'outputDir'.",task.outputDir));
}
