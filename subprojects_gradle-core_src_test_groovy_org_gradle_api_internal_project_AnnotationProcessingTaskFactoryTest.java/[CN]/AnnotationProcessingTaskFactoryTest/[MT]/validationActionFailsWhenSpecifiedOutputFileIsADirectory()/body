{
  TaskWithOutputFile task=expectTaskCreated(new TaskWithOutputFile(existingDir));
  assertValidationFails(task,String.format("Cannot write to file '%s' specified for property 'outputFile' as it is a directory.",task.outputFile));
}
