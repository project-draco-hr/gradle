{
  Clock clock=new Clock();
  new CompositeStoppable().add(stores.values()).add(oldModelCache).add(newModelCache).stop();
  LOG.debug("Deleted {} resolution results binary files in {}",stores.size(),clock.getTime());
  oldModelCache=null;
  newModelCache=null;
}
