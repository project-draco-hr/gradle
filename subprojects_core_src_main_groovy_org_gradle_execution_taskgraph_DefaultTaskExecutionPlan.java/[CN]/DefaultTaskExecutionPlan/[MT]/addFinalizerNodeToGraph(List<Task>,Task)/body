{
  TaskInfo finalizerNode=graph.getNode(finalizerTask);
  if (finalizerNode == null) {
    finalizerNode=addEntryTask(finalizerTask);
    finalizerNode.shouldNotRun();
    queue.add(finalizerTask);
  }
  return finalizerNode;
}
