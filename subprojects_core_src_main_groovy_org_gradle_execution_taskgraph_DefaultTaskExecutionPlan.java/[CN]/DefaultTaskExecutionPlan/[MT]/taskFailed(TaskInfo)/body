{
  lock.lock();
  try {
    taskInfo.executionFailed();
    try {
      failureHandler.onTaskFailure(taskInfo.getTask());
    }
 catch (    Exception e) {
      abortExecution(e);
    }
    condition.signalAll();
  }
  finally {
    lock.unlock();
  }
}
