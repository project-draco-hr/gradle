{
  TaskInfo finaliserNode=graph.getNode(finaliserTask);
  if (finaliserNode == null) {
    finaliserNode=addEntryTask(finaliserTask);
    finaliserNode.shouldNotRun();
    queue.add(finaliserTask);
  }
  return finaliserNode;
}
