{
  File project=new File(getProject().getProjectDir(),EclipseProject.PROJECT_FILE_NAME);
  assertTrue(project.isFile());
  assertThat(GFileUtils.readFileToString(project),Matchers.equalTo(IOUtils.toString(this.getClass().getResourceAsStream(expectedResourcePath))));
}
