{
  delegate=new NotationParserBuilder<T>().resultingType(optionType).parsers(Arrays.asList(new EnumFromStringNotationParser<T>(optionType))).invalidNotationMessage(String.format("Converting String to $s",optionType)).toComposite();
}
