{
  new DaemonStartupCommunication().printDaemonStarted(originalOut,pid,daemonLog);
  try {
    originalOut.close();
    originalErr.close();
  }
  finally {
    originalOut=null;
    originalErr=null;
  }
}
