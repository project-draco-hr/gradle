{
  for (  Configuration configuration : configurations) {
    for (    Dependency dependency : configuration.getDependencies()) {
      dependencyDescriptorFactory.addDependencyDescriptor(configuration.getName(),moduleDescriptor,dependency,clientModuleRegistry);
    }
  }
}
