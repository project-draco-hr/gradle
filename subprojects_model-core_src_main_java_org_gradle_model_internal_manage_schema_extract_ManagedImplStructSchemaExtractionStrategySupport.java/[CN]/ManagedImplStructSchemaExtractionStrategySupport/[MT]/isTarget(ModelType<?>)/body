{
  if (!type.getRawClass().isAnnotationPresent(Managed.class)) {
    return false;
  }
  return implementedInterface == null || (!type.getRawClass().equals(implementedInterface) && implementedInterface.isAssignableFrom(type.getRawClass()));
}
