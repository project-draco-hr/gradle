{
  forkControlLock.lock();
  try {
    if (startAllowed()) {
      startFork(forkInfo);
    }
 else {
      pendingForkStartRequests.add(forkInfo);
    }
  }
  finally {
    forkControlLock.unlock();
  }
}
