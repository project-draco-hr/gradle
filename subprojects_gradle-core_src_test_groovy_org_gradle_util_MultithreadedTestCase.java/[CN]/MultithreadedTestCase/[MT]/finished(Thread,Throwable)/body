{
  lock.lock();
  try {
    LOGGER.debug("Finished {}",thread);
    active.remove(thread);
    if (failure != null) {
      failures.add(failure);
    }
    activeChanged.signalAll();
  }
  finally {
    lock.unlock();
  }
}
