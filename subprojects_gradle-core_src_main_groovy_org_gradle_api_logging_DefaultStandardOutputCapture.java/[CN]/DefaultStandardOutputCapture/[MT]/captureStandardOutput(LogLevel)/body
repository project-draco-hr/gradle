{
  if (enabled && this.level == level) {
    return this;
  }
  enabled=true;
  this.level=level;
  if (globalState != null) {
    StandardOutputLogging.flush();
    StandardOutputLogging.on(level);
  }
  return this;
}
