{
  dependencyPublisher.publish(configurations,resolvers,moduleDescriptorConverter.convert(this,true),uploadModuleDescriptor,getProject().getBuildDir(),this,ivy(resolvers.getResolverList()).getPublishEngine());
}
