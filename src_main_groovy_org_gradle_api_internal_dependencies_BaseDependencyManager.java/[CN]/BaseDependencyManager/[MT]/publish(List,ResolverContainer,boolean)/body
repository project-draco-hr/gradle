{
  dependencyPublisher.publish(configurations,resolvers,moduleDescriptorConverter.convert(this),uploadModuleDescriptor,new File(getProject().getBuildDir(),"ivy.xml"),this,ivy(resolvers.getResolverList()).getPublishEngine());
}
