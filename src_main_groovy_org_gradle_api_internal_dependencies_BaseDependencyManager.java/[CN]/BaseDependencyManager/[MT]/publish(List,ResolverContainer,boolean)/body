{
  dependencyPublisher.publish(configurations,resolvers,moduleDescriptorConverter.convert(this,true),uploadModuleDescriptor,new File(getProject().getBuildDir(),"ivy.xml"),this,ivy(resolvers.getResolverList()).getPublishEngine());
}
