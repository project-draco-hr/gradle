{
  if (configuration == null) {
    return null;
  }
  return configurationResolverFactory.createConfigurationResolver(configuration,dependencyContainer,classpathResolvers,artifactContainer,configurationContainer);
}
