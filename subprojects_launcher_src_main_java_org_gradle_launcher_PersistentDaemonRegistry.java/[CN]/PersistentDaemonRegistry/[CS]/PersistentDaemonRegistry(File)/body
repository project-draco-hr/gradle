{
  this.daemonDir=new DaemonDir(baseFolder);
  cache=new SimpleStateCache<DaemonRegistryFile>(daemonDir.registryFile,new OnDemandFileLock(daemonDir.getRegistry(),"daemon addresses registry",new DefaultFileLockManager()),new DefaultSerializer<DaemonRegistryFile>());
}
