{
  DefaultObjectConfigurationAction action=new DefaultObjectConfigurationAction(fileResolver,services.get(ScriptPluginFactory.class),this);
  ConfigureUtil.configure(options,action);
  action.execute();
}
