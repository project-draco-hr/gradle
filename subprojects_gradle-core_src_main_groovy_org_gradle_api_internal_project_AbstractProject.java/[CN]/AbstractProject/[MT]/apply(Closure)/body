{
  DefaultObjectConfigurationAction action=new DefaultObjectConfigurationAction(fileResolver,services.get(ScriptObjectConfigurerFactory.class),this);
  configure(action,closure);
  action.execute();
}
