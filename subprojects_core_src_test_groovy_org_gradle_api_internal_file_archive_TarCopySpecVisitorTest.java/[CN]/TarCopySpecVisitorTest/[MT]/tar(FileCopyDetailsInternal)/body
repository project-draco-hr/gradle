{
  visitor.startVisit();
  visitor.visitSpec(copySpec);
  for (  FileCopyDetailsInternal f : files) {
    if (f.isDirectory()) {
      visitor.visit(f);
    }
 else {
      visitor.visit(f);
    }
  }
  visitor.endVisit();
}
