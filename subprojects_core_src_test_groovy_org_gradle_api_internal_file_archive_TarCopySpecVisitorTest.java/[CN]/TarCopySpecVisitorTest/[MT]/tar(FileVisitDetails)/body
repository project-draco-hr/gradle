{
  visitor.startVisit(copyAction);
  visitor.visitSpec(copySpec);
  for (  FileVisitDetails f : files) {
    if (f.isDirectory()) {
      visitor.visitDir(f);
    }
 else {
      visitor.visitFile(f);
    }
  }
  visitor.endVisit();
}
