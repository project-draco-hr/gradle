{
  visitor.startVisit();
  visitor.visitSpec(copySpec);
  for (  FileCopyDetails f : files) {
    if (f.isDirectory()) {
      visitor.visitDir(f);
    }
 else {
      visitor.visitFile(f);
    }
  }
  visitor.endVisit();
}
