{
  visitor.startVisit();
  visitor.visitSpec(copySpec);
  for (  FileCopyDetails f : files) {
    if (f.isDirectory()) {
      visitor.visit(f);
    }
 else {
      visitor.visit(f);
    }
  }
  visitor.endVisit();
}
