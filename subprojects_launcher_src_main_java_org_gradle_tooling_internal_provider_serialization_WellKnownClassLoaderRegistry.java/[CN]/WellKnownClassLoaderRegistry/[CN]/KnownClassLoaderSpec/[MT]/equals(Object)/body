{
  if (obj == this) {
    return true;
  }
  if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  KnownClassLoaderSpec other=(KnownClassLoaderSpec)obj;
  return id == other.id;
}
