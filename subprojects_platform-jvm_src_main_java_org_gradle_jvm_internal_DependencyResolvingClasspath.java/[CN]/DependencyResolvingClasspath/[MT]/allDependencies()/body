{
  return new Iterable<DependencySpec>(){
    @Override public Iterator<DependencySpec> iterator(){
      return Iterables.concat(sourceSetDependencies(),apiDependencies()).iterator();
    }
  }
;
}
