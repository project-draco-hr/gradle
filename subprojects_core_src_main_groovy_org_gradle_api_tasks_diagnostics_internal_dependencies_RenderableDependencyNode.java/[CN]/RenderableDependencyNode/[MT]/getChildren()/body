{
  if (dependencyNode == null) {
    return Sets.newHashSet();
  }
  return new LinkedHashSet(Collections2.transform(dependencyNode.getDependencies(),new Function<DependencyGraphNode,Object>(){
    public Object apply(    DependencyGraphNode input){
      return new RenderableDependencyNode(input);
    }
  }
));
}
