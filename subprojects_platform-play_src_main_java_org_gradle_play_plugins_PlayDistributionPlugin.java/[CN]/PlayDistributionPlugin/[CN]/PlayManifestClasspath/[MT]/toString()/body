{
  return Joiner.on(" ").join(Iterables.transform(Iterables.concat(playConfiguration.getAllArtifacts(),Collections.singleton(assetsJarFile)),new PlayPluginConfigurations.Renamer(playConfiguration)));
}
