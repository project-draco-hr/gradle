{
  return Joiner.on(" ").join(Iterables.transform(Iterables.concat(playConfiguration.getFileCollection(),Collections.singleton(assetsJarFile)),new Function<File,String>(){
    @Override public String apply(    File input){
      return input.getName();
    }
  }
));
}
