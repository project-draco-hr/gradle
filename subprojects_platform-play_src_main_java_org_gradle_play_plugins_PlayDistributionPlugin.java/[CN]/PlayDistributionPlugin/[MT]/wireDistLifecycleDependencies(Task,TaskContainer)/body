{
  distTask.dependsOn(tasks.withType(Zip.class).matching(new Spec<Zip>(){
    @Override public boolean isSatisfiedBy(    Zip zipTask){
      return DISTRIBUTION_GROUP.equals(zipTask.getGroup());
    }
  }
));
}
