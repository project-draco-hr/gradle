{
  stageTask.dependsOn(tasks.withType(Copy.class).matching(new Spec<Copy>(){
    @Override public boolean isSatisfiedBy(    Copy copyTask){
      return DISTRIBUTION_GROUP.equals(copyTask.getGroup());
    }
  }
));
}
