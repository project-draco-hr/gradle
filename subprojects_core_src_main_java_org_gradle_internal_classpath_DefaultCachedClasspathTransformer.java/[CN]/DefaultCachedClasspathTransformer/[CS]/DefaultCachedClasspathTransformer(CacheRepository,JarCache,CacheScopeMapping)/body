{
  this.jarCache=jarCache;
  this.cacheScopeMapping=cacheScopeMapping;
  this.cache=cacheRepository.cache("jars-1").withDisplayName("jars").withCrossVersionCache().withLockOptions(mode(FileLockManager.LockMode.None)).open();
  this.alreadyCachedSpec=new AlreadyCachedSpec();
}
