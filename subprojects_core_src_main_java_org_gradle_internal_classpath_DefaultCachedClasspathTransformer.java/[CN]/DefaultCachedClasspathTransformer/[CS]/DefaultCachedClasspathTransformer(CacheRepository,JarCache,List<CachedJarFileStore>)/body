{
  this.cache=cacheRepository.cache("jars-2").withDisplayName("jars").withCrossVersionCache().withLockOptions(mode(FileLockManager.LockMode.None)).open();
  this.jarFileTransformer=new CachedJarFileTransformer(jarCache,cache,fileStores);
}
