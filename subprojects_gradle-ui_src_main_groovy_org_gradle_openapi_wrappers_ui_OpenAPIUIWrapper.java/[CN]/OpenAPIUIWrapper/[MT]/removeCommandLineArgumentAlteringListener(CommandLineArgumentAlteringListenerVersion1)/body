{
  CommandLineArgumentAlteringListenerWrapper wrapper=commandLineListenerMap.remove(listener);
  if (wrapper != null)   singlePaneUIInstance.getGradlePluginLord().removeCommandLineArgumentAlteringListener(wrapper);
}
