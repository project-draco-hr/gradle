{
  TaskWithOutputFiles task=expectTaskCreated(TaskWithOutputFiles.class,Collections.singletonList(existingDir));
  assertValidationFails(task,String.format("Cannot write to file '%s' specified for property 'outputFiles' as it is a directory.",task.outputFiles.get(0)));
}
