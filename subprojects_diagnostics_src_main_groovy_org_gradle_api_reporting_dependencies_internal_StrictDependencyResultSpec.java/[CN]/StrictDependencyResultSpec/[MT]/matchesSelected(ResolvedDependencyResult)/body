{
  ModuleComponentIdentifier selected=(ModuleComponentIdentifier)candidate.getSelected().getId();
  return selected.getGroup().equals(moduleIdentifier.getGroup()) && selected.getModule().equals(moduleIdentifier.getName());
}
