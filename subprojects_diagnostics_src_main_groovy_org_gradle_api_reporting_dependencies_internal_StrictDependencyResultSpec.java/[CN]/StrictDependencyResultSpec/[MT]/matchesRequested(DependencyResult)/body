{
  ModuleComponentSelector requested=(ModuleComponentSelector)candidate.getRequested();
  return requested.getGroup().equals(moduleIdentifier.getGroup()) && requested.getName().equals(moduleIdentifier.getName());
}
