{
  ModuleComponentSelector requested=(ModuleComponentSelector)candidate.getRequested();
  return requested.getGroup().equals(moduleIdentifier.getGroup()) && requested.getModule().equals(moduleIdentifier.getName());
}
