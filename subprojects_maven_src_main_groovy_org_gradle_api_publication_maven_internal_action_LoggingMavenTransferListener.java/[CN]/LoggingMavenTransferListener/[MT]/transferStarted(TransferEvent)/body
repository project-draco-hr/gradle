{
  long contentLength=event.getResource().getContentLength();
  if (contentLength > 0) {
    LOGGER.info("Transferring " + ((contentLength + KILO / 2) / KILO) + "K from "+ event.getWagon().getRepository().getId());
  }
}
