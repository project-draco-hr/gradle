{
  boolean wasStarted=lockCache(operationDisplayName);
  try {
    if (!wasStarted) {
      onStartWork();
    }
    try {
      return action.create();
    }
  finally {
      if (!wasStarted) {
        onEndWork();
      }
    }
  }
  finally {
    unlockCache(wasStarted);
  }
}
