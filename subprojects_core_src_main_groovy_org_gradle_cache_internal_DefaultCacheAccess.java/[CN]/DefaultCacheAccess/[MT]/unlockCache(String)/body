{
  lock.lock();
  try {
    operationStack.get().popCacheAction(operationDisplayName);
    if (!operationStack.get().isInCacheAction()) {
      owner=null;
      condition.signalAll();
    }
  }
  finally {
    lock.unlock();
  }
}
