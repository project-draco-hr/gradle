{
  try {
    cacheClosedCount++;
    for (    MultiProcessSafePersistentIndexedCache<?,?> cache : caches) {
      cache.close();
    }
    fileLock.close();
  }
  finally {
    fileLock=null;
    contended=false;
  }
}
