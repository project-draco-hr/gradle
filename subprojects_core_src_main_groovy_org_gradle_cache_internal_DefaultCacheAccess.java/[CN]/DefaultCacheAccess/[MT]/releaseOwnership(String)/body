{
  lock.lock();
  try {
    operations.popCacheAction(operationDisplayName);
    if (!operations.isInCacheAction()) {
      owner=null;
      condition.signalAll();
    }
  }
  finally {
    lock.unlock();
  }
}
