{
  lock.lock();
  try {
    operationStack.popCacheAction(operationDisplayName);
    if (!operationStack.isInCacheAction()) {
      owner=null;
      condition.signalAll();
    }
  }
  finally {
    lock.unlock();
  }
}
