{
  if (fileLock == null) {
    return false;
  }
  try {
    if (fileLock.isContended() || fileLock.getMode() == Shared) {
      closeFileLock(fileLock);
    }
 else {
      fileLock.setBusy(false);
    }
  }
  finally {
    fileLock=null;
  }
  return true;
}
