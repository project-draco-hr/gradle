{
  lock.lock();
  try {
    operations.close();
    if (fileLock != null) {
      closeFileLock();
    }
    if (cacheClosedCount != 1) {
      LOG.debug("Cache {} was closed {} times.",cacheDiplayName,cacheClosedCount);
    }
  }
  finally {
    lockMode=null;
    owner=null;
    lock.unlock();
  }
}
