{
  state.assertCanReadChildren();
  if (elements == null) {
    elements=Sets.newHashSet(Iterables.transform(modelNode.getLinks(elementType),new Function<MutableModelNode,T>(){
      @Override public T apply(      MutableModelNode input){
        return input.asReadOnly(elementType,descriptor).getInstance();
      }
    }
));
  }
  return elements;
}
