{
  DefaultObjectConfigurationAction action=new DefaultObjectConfigurationAction(getFileResolver(),services.get(ScriptPluginFactory.class),this);
  ConfigureUtil.configure(closure,action);
  action.execute();
}
