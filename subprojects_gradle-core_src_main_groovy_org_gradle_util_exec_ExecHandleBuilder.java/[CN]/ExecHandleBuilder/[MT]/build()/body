{
  if (StringUtils.isEmpty(getExecutable())) {
    throw new IllegalStateException("execCommand == null!");
  }
  return new DefaultExecHandle(getWorkingDir(),getExecutable(),getArguments(),normalTerminationExitCode,getEnvironment(),standardOutput,errorOutput,input,listeners);
}
