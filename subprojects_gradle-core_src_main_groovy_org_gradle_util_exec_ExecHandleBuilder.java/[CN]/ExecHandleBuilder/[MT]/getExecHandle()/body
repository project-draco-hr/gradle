{
  if (StringUtils.isEmpty(execCommand)) {
    throw new IllegalStateException("execCommand == null!");
  }
  return new DefaultExecHandle(execDirectory,execCommand,arguments,normalTerminationExitCode,environment,keepWaitingTimeout,standardOutput,errorOutput,input,listeners);
}
