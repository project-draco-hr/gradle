{
  if (StringUtils.isEmpty(execCommand)) {
    throw new IllegalStateException("execCommand == null!");
  }
  return new DefaultExecHandle(execDirectory,execCommand,arguments,normalTerminationExitCode,environment,keepWaitingTimeout,standardOutputHandle,errorOutputHandle,notifierFactory,listeners);
}
