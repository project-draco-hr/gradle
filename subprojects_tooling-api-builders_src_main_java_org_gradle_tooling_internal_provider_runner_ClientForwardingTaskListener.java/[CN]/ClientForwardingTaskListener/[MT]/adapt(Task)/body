{
  return new DefaultTaskDescriptor(EventIdGenerator.generateId(taskDescriptor),taskDescriptor.getPath(),taskDescriptor.getName(),taskDescriptor.getDescription(),taskDescriptor.getProject().getPath());
}
