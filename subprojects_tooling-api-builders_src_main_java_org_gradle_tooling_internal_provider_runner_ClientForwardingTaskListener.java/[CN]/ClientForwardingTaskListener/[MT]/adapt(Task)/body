{
  return new DefaultTaskDescriptor(EventIdGenerator.generateId(taskDescriptor),taskDescriptor.getName(),taskDescriptor.getDescription(),taskDescriptor.getPath(),taskDescriptor.getProject().getPath());
}
