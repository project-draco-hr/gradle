{
  return bridgeNamedDomainObjectCollection(containerType,publicType,itemType,modelPath,container,new Named.Namer(),descriptor,new Transformer<String,String>(){
    @Override public String transform(    String s){
      return descriptor + '.' + s+ "()";
    }
  }
);
}
