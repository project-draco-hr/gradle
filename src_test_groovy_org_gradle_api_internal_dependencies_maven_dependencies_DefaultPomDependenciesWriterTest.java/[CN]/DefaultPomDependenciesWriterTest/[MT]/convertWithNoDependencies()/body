{
  context.checking(new Expectations(){
{
      one(dependenciesConverterMock).convert(pomMock);
      will(returnValue(new ArrayList()));
    }
  }
);
  dependenciesWriter.convert(pomMock,testPrintWriter);
  assertEquals("",testStringWriter.toString());
}
