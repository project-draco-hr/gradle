{
  int index=pattern.indexOf(REVISION_TOKEN);
  boolean patternStartsWithRevisionToken=index == 0;
  return (pattern.length() <= index + REV_TOKEN_LENGTH) || fileSep.equals(pattern.substring(index + REV_TOKEN_LENGTH,index + REV_TOKEN_LENGTH + 1)) && (patternStartsWithRevisionToken || fileSep.equals(pattern.substring(index - 1,index)));
}
