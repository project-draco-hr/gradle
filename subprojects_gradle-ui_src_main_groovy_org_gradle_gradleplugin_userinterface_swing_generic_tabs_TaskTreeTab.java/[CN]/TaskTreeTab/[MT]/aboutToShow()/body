{
  resetShowDescription();
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      if (gradlePluginLord.isSetupComplete())       refresh();
 else       showTextInViewport("Cannot show tasks until configuration is complete. See Setup tab.");
    }
  }
);
}
