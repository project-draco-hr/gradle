{
  if (toggleFilterButton.isSelected())   treeComponent.populate(editor.createFilter());
 else   treeComponent.populate(new AllowAllProjectAndTaskFilter());
  treeComponent.getTree().setBackground(defaultTreeBackground);
  showTreeInViewport();
}
