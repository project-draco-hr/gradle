{
  options.sourcepath(existingSourceDirs).directory(getDestinationDir()).classpath(getClasspath()).windowTitle(getTitle());
  if (maxMemory != null)   options.jFlags("-Xmx" + maxMemory);
  options.toOptionsFile(new BufferedWriter(new FileWriter(javadocOptionsFile)));
}
