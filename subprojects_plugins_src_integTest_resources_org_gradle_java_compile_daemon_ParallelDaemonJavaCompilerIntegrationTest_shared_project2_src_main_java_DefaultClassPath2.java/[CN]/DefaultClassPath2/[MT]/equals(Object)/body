{
  if (obj == this) {
    return true;
  }
  if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  DefaultClassPath2 other=(DefaultClassPath2)obj;
  return files.equals(other.files);
}
