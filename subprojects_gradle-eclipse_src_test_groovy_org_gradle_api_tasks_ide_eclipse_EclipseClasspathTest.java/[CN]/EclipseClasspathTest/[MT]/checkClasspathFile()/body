{
  File classpath=new File(getProject().getProjectDir(),EclipseClasspath.CLASSPATH_FILE_NAME);
  assertTrue(classpath.isFile());
  assertThat(GFileUtils.readFileToString(classpath),Matchers.equalTo(IOUtils.toString(this.getClass().getResourceAsStream("expectedClasspathFile.txt"))));
}
