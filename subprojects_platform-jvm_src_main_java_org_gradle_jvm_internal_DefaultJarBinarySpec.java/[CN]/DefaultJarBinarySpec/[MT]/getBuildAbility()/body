{
  if (buildAbility == null) {
    buildAbility=new CompositeBuildAbility(super.getBuildAbility(),new ToolSearchBuildAbility(((JavaToolChainInternal)getToolChain()).select(getTargetPlatform())));
  }
  return buildAbility;
}
