{
  List<IvyModuleArtifactPublishMetaData> artifacts=Lists.newArrayList();
  for (  Map.Entry<IvyArtifactName,File> entry : artifactFiles.entrySet()) {
    IvyArtifactName artifactName=entry.getKey();
    File artifactFile=entry.getValue();
    artifacts.add(new DefaultIvyModuleArtifactPublishMetaData(id,artifactName,artifactFile));
  }
  return artifacts;
}
