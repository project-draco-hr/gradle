{
  String role=type.getName();
  try {
    @SuppressWarnings("unchecked") T lookup1=(T)getContainer().lookup(role);
    return lookup1;
  }
 catch (  ComponentLookupException e) {
    throw new GradleException("Unable to find component: " + role,e);
  }
}
