{
  if (ObjectiveCCompile.class.isAssignableFrom(task.getClass()) || ObjectiveCppCompile.class.isAssignableFrom(task.getClass())) {
    return new RegexBackedIncludesImportsParser();
  }
 else {
    return new DefaultRegexBackedIncludesParser();
  }
}
