{
  if (obj == this) {
    return true;
  }
  if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  DefaultClassPath1 other=(DefaultClassPath1)obj;
  return files.equals(other.files);
}
