{
  try {
    line=in.readUTF();
    return true;
  }
 catch (  EOFException e) {
    IOUtils.closeQuietly(in);
    return false;
  }
catch (  IOException e) {
    IOUtils.closeQuietly(in);
    throw UncheckedException.throwAsUncheckedException(e);
  }
}
