{
  ImmutableMap attributes=ImmutableMap.builder().put("type","module").put("module-name",name).putAll(getAttributeMapForScopeAndExported()).build();
  parentNode.appendNode("orderEntry",attributes);
}
