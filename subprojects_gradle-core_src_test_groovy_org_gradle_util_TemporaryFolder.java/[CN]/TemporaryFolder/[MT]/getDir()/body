{
  if (!initialised) {
    if (dir == null) {
      String prefix=determinePrefix();
      dir=root.file(prefix);
    }
    GradleUtil.deleteDir(dir);
    dir.mkdirs();
    initialised=true;
  }
  return dir;
}
