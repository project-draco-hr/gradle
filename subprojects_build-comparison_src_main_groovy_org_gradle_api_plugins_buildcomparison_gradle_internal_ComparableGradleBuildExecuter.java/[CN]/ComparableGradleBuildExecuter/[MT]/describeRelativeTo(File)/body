{
  return "dir: '" + GFileUtils.relativePath(relativeTo,getSpec().getProjectDir()) + "'"+ ", tasks: '"+ GUtil.join(getSpec().getTasks()," ")+ "'"+ ", arguments: '"+ GUtil.join(getSpec().getArguments()," ")+ "'"+ ", gradleVersion: "+ getSpec().getGradleVersion();
}
