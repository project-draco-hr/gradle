{
  final Project root=context.mock(Project.class);
  context.checking(new Expectations(){
{
      allowing(root).getTasksByName("compil",true);
      will(returnValue(new HashSet()));
    }
  }
);
  buildExecuter.execute("compil",true,root,root);
}
