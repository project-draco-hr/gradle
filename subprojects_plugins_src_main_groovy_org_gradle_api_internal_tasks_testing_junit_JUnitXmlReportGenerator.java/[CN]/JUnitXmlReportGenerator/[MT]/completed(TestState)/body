{
  if (!state.equals(testSuite)) {
    xmlTestsuiteWriter.addTestCase(state.test.getName(),state.resultType,state.getExecutionTime(),state.failures);
  }
 else {
    xmlTestsuiteWriter.writeSuiteData(state.getExecutionTime());
    testSuite=null;
  }
}
