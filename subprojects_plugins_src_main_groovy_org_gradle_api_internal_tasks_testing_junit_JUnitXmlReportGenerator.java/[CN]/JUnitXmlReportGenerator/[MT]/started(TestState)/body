{
  TestDescriptorInternal test=state.test;
  if (test.getName().equals(test.getClassName())) {
    xmlTestsuiteWriter=factory.create(testResultsDir,test.getClassName(),state.getStartTime());
    testSuite=state;
  }
}
