{
  lock.lock();
  try {
    listenerManager.removeListener(buildListener);
    CompositeStoppable.stoppable(handles.values()).stop();
  }
  finally {
    stopped=true;
    handles.clear();
    lock.unlock();
  }
}
