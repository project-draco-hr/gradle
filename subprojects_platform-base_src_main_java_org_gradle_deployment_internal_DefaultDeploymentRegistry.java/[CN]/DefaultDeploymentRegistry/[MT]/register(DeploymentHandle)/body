{
  lock.lock();
  try {
    if (!handles.containsKey(handle.getId())) {
      handles.put(handle.getId(),handle);
      return true;
    }
 else {
      return false;
    }
  }
  finally {
    lock.unlock();
  }
}
