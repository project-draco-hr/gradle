{
  if (stateIn(ExecHandleState.SUCCEEDED,ExecHandleState.FAILED))   throw new IllegalStateException("not in finished or failed state!");
  endStateInfoLock.lock();
  try {
    return exitCode;
  }
  finally {
    endStateInfoLock.unlock();
  }
}
