{
  final List<JdkSpec> asImmutable=Lists.newArrayList();
  jdks.afterEach(new Action<JdkSpec>(){
    @Override public void execute(    JdkSpec jdkSpec){
      asImmutable.add(jdkSpec);
    }
  }
);
  return asImmutable;
}
