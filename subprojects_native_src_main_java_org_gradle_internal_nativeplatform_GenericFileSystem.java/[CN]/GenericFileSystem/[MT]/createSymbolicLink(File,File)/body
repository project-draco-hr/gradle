{
  int returnCode=doCreateSymbolicLink(link,target);
  if (returnCode != 0) {
    throw new IOException("Failed to create symbolic link " + link + " pointing to "+ target+ ". Return code is: "+ returnCode);
  }
}
