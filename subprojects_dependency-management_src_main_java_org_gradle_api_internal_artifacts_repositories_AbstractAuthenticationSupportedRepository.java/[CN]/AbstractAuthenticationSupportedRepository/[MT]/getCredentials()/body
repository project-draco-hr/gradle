{
  if (passwordCredentials == null && alternativeCredentials != null) {
    throw new IllegalStateException("Password credentials has been overridden by a specific credentials of type [" + alternativeCredentials.getClass().getName() + "] Credentials should be accessed using 'getAlternativeCredentials()'");
  }
  return passwordCredentials;
}
