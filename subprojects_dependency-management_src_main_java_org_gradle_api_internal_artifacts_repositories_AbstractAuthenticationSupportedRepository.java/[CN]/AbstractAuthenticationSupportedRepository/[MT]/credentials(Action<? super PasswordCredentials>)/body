{
  if (passwordCredentials == null) {
    throw new IllegalStateException("Password credentials is null, most likely an alternative " + "credentials type has been configured for this repository");
  }
  action.execute(passwordCredentials);
}
