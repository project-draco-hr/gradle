{
  return new DefaultProject(name,parent,rootDir,rootProject,buildFileName,buildScriptClassLoader,this,dependencyManagerFactory.createDependencyManager(),buildScriptProcessor,pluginRegistry,projectRegistry);
}
