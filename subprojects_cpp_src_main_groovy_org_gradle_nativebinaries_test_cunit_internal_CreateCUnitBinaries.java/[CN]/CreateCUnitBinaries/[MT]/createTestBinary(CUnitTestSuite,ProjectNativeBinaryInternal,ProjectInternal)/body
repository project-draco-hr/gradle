{
  BinaryNamingScheme namingScheme=new DefaultBinaryNamingSchemeBuilder(testedBinary.getNamingScheme()).withComponentName(cUnitTestSuite.getBaseName()).withTypeString("CUnitExe").build();
  ProjectNativeBinary testBinary=instantiator.newInstance(DefaultTestSuiteExecutableBinary.class,cUnitTestSuite,testedBinary.getFlavor(),testedBinary.getToolChain(),testedBinary.getTargetPlatform(),testedBinary.getBuildType(),namingScheme,resolver);
  setupDefaults(testBinary,project);
  return testBinary;
}
