{
  BinaryNamingScheme namingScheme=new DefaultBinaryNamingSchemeBuilder(testedBinary.getNamingScheme()).withComponentName(cUnitTestSuite.getBaseName()).withTypeString("CUnitExe").build();
  ProjectNativeBinary testBinary=instantiator.newInstance(DefaultCUnitTestSuiteExecutableBinary.class,cUnitTestSuite,testedBinary.getFlavor(),testedBinary.getToolChain(),testedBinary.getTargetPlatform(),testedBinary.getBuildType(),namingScheme,resolver);
  setupDefaults(testBinary,project);
  return testBinary;
}
