{
  BinaryNamingScheme namingScheme=new DefaultBinaryNamingSchemeBuilder(testedBinary.getNamingScheme()).withComponentName(cUnitTestSuite.getBaseName()).withTypeString("CUnitExe").build();
  CUnitTestSuiteBinary testBinary=instantiator.newInstance(DefaultCUnitTestSuiteBinary.class,cUnitTestSuite,testedBinary.getFlavor(),testedBinary.getToolChain(),testedBinary.getTargetPlatform(),testedBinary.getBuildType(),namingScheme,resolver);
  setupDefaults(testBinary,project);
  return testBinary;
}
