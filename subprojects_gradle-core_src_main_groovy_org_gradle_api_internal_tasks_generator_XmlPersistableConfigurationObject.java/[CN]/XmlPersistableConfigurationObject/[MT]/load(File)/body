{
  try {
    FileReader reader=new FileReader(inputFile);
    try {
      load(reader);
    }
  finally {
      reader.close();
    }
  }
 catch (  Exception e) {
    throw UncheckedException.asUncheckedException(e);
  }
}
