{
  if (!initialised) {
    locateInstallsInRegistry();
    locateInstallInPath();
    initialised=true;
  }
  if (candidate != null) {
    return locateUserSpecifiedInstall(candidate);
  }
  return determineDefaultInstall();
}
