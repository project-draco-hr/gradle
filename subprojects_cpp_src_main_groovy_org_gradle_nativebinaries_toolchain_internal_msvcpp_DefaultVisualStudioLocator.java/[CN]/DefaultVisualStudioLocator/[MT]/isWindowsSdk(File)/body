{
  return (new File(candidate,RESOURCE_PATH + RESOURCE_FILENAME).isFile() || new File(candidate,RESOURCE_PATH_WINSDK8 + RESOURCE_FILENAME).isFile()) && (new File(candidate,KERNEL32_PATH + KERNEL32_FILENAME).isFile() || new File(candidate,KERNEL32_PATH_WINSDK8 + KERNEL32_FILENAME).isFile());
}
