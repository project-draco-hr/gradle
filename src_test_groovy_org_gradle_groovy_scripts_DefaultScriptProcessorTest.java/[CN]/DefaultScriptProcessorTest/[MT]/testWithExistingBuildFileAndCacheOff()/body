{
  createBuildScriptFile();
  context.checking(new Expectations(){
{
      one(scriptHandlerMock).createScript(TEST_SCRIPT_TEXT + System.getProperty("line.separator") + TEST_SCRIPT_ATACHEMENT,testClassLoader,TEST_SCRIPT_NAME,expectedScriptBaseClass);
      will(returnValue(expectedScript));
    }
  }
);
  assertSame(expectedScript,scriptProcessor.createScriptFromFile(testCacheDir,testScriptFile,TEST_SCRIPT_ATACHEMENT,CacheUsage.OFF,testClassLoader,expectedScriptBaseClass));
}
