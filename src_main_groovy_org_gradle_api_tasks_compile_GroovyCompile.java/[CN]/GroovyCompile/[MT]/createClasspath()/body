{
  return GUtil.addLists(classpathConverter.createFileClasspath(getProject().getRootDir(),getUnmanagedClasspath()),getDependencyManager().configuration(getResolveInstruction().getConfiguration()).resolve(getResolveInstruction()));
}
