{
  try {
    Resource resource=repository.getResource(path);
    try {
      return resource.exists();
    }
  finally {
      discardResource(resource);
    }
  }
 catch (  IOException e) {
    return false;
  }
}
