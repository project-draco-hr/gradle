{
  try {
    Resource resource=repository.getResource(path);
    try {
      return resource.exists();
    }
  finally {
      cleanupResource(resource);
    }
  }
 catch (  IOException e) {
    return false;
  }
}
