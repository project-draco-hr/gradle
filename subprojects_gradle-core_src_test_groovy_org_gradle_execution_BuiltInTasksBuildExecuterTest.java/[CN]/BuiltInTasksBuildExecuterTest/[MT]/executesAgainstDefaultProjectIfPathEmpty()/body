{
  executer.setOptions(BuiltInTasksBuildExecuter.Options.DEPENDENCIES);
  executer.select(gradle);
  assertThat(((DependencyReportTask)executer.getTask()).getProjects(),equalTo(WrapUtil.<Project>toSet(gradle.getDefaultProject())));
}
