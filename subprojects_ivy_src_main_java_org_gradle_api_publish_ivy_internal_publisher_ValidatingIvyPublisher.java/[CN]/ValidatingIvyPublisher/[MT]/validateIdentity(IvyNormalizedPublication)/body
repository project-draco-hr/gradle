{
  IvyPublicationIdentity identity=publication.getProjectIdentity();
  Node infoNode=getIvyFileInfoNode(publication.getDescriptorFile());
  field(publication,"organisation",identity.getOrganisation()).notEmpty().matches((String)infoNode.attribute("organisation"));
  field(publication,"module name",identity.getModule()).notEmpty().matches((String)infoNode.attribute("module"));
  field(publication,"revision",identity.getRevision()).notEmpty().matches((String)infoNode.attribute("revision"));
}
