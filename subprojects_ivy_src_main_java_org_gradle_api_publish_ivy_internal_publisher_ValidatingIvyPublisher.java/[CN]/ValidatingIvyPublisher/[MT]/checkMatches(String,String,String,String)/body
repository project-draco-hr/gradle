{
  if (!projectIdentityValue.equals(pomFileValue)) {
    throw new InvalidUserDataException(String.format("Invalid publication '%s': supplied %s does not match ivy descriptor (cannot edit %2$s directly in the ivy descriptor file).",publicationName,name));
  }
}
