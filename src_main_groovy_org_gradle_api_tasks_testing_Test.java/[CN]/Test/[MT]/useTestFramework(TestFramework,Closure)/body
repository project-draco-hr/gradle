{
  if (testFramework == null)   throw new IllegalArgumentException("testFramework is null!");
  testFramework.initialize(getProject(),this);
  this.testFramework=testFramework;
  if (testFrameworkConfigure != null)   ConfigureUtil.configure(testFrameworkConfigure,testFramework.getOptions());
  return testFramework;
}
