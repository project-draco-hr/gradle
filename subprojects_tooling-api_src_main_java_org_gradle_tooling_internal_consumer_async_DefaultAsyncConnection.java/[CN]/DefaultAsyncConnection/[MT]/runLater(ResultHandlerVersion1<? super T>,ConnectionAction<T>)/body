{
  onStartOperation();
  executor.execute(new Runnable(){
    public void run(){
      T result;
      try {
        result=action.run();
      }
 catch (      Throwable t) {
        handler.onFailure(t);
        return;
      }
      handler.onComplete(result);
    }
  }
);
}
