{
  long lastModified=12345;
  boolean createdSuccessfully=true;
  DefaultOutputHistory outputHistory=new DefaultOutputHistory(createdSuccessfully,lastModified);
  assertThat(outputHistory.wasCreatedSuccessfully(),equalTo(createdSuccessfully));
  assertThat(outputHistory.getLastModified(),equalTo(lastModified));
}
