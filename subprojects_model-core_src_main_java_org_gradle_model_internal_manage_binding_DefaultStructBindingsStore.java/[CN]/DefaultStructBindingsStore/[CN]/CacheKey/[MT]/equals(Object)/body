{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  CacheKey cacheKey=(CacheKey)o;
  return Objects.equal(publicSchema,cacheKey.publicSchema) && Objects.equal(viewSchemas,cacheKey.viewSchemas) && Objects.equal(delegateSchema,cacheKey.delegateSchema);
}
