{
  this(null,pluginInspector,CacheBuilder.newBuilder().build(new PotentialPluginCacheLoader(pluginInspector)),CacheBuilder.newBuilder().build(new PluginIdCacheLoader()),Factories.constant(classLoader));
}
