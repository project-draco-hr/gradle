{
  if (obj == this) {
    return true;
  }
  if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  DefaultModuleVersionArtifactIdentifier other=(DefaultModuleVersionArtifactIdentifier)obj;
  return other.moduleVersionIdentifier.equals(moduleVersionIdentifier) && other.name.equals(name) && other.type.equals(type)&& Objects.equals(other.extension,extension)&& other.attributes.equals(attributes);
}
