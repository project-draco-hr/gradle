{
  server.requestShutdown();
  if (!hasCompletedConnection)   reportPrematureClientExit(returnCode,"Failed to connect to gradle process for command '" + commandLine + "'\n"+ output);
 else   if (!hasReceivedBuildCompleteNotification)   reportPrematureClientExit(returnCode,output);
}
