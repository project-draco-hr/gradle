{
  doCreate(name,ModelType.of(type),new Factory<S>(){
    @Override public S create(){
      S element=instantiator.create(name,type);
      configAction.execute(element);
      target.add(element);
      return element;
    }
  }
);
}
