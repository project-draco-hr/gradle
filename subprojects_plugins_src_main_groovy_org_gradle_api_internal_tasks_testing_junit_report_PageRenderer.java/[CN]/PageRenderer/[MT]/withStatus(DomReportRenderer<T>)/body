{
  return new DomReportRenderer<T>(){
    @Override public void render(    T model,    Element parent){
      parent.setAttribute("class",model.getStatusClass());
      renderer.render(model,parent);
    }
  }
;
}
