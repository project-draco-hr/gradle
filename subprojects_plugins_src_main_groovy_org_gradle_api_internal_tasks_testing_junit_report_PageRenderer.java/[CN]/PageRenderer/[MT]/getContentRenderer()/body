{
  return new DomReportRenderer<T>(){
    @Override public void render(    T model,    Element content){
      PageRenderer.this.results=model;
      registerTabs();
      renderTabs(content);
    }
  }
;
}
