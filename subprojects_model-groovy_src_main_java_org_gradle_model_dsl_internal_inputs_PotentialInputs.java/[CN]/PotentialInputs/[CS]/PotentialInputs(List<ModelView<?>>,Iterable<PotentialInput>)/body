{
  this.modelViews=modelViews;
  this.inputs=Maps.uniqueIndex(inputs,new Function<PotentialInput,String>(){
    @Override public String apply(    PotentialInput input){
      return input.getConsumerPath();
    }
  }
);
}
