{
  this.sourceFile=sourceFile;
  Matcher m=BASENAME_PATTERN.matcher(sourceFile.getName());
  if (m.find()) {
    baseName=m.group(1);
  }
 else {
    throw new IllegalArgumentException("unrecognized file extension for '" + sourceFile.getName() + "', expected .g or .G");
  }
}
