{
  if (this == o) {
    return true;
  }
  if (!(o instanceof Path)) {
    return false;
  }
  Path path=(Path)o;
  if (!canonicalUrl.equals(path.canonicalUrl)) {
    return false;
  }
  return true;
}
