{
  try {
    for (    PropertyDescriptor prop : Introspector.getBeanInfo(clazz).getPropertyDescriptors())     if (key.equals(prop.getName()))     return prop;
  }
 catch (  IntrospectionException e) {
  }
  return null;
}
