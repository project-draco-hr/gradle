{
  try {
    for (    PropertyDescriptor prop : Introspector.getBeanInfo(clazz,java.lang.Object.class).getPropertyDescriptors())     if (key.equals(prop.getName()))     return prop;
  }
 catch (  IntrospectionException e) {
    throw new RuntimeException(e);
  }
  return null;
}
