{
  if (stack.isEmpty()) {
    return null;
  }
 else {
    WorkingSet<ModelProperty<?>> childSet=stack.peek();
    if (childSet.hasNext()) {
      return childSet.next();
    }
 else {
      stack.pop();
      return next();
    }
  }
}
