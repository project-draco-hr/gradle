{
  super(adapter,versionDetails,modelMapping);
  this.delegate=delegate;
  mapper=Actions.composite(new PropertyHandlerFactory().forVersion(versionDetails),new TaskSelectorsPropertyHandlerFactory().forVersion(versionDetails));
}
