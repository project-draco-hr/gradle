{
  ArtifactId artifactId=rule.getId();
  boolean matchesRule=MatcherHelper.matches(rule.getMatcher(),artifactId.getModuleId(),element);
  return !(matchesRule && matchesAnyExpression(artifactId.getName()) && matchesAnyExpression(artifactId.getType())&& matchesAnyExpression(artifactId.getExt()));
}
