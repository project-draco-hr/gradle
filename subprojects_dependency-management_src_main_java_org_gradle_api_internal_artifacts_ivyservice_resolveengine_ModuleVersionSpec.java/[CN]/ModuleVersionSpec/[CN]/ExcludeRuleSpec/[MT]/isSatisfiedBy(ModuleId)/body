{
  ArtifactId artifactId=rule.getId();
  return MatcherHelper.matches(rule.getMatcher(),artifactId.getModuleId(),element) && matchesAnyExpression(artifactId.getName()) && matchesAnyExpression(artifactId.getType())&& matchesAnyExpression(artifactId.getExt());
}
