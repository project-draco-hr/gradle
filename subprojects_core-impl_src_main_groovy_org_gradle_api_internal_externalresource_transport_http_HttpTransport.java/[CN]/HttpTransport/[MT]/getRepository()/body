{
  HttpClientHelper http=new HttpClientHelper(new DefaultHttpSettings(credentials));
  final HttpResourceAccessor accessor=new HttpResourceAccessor(http);
  return new DefaultExternalResourceRepository(name,new ProgressLoggingExternalResourceAccessor(accessor,progressLoggerFactory),new HttpResourceUploader(http),new HttpResourceLister(accessor));
}
