{
  if (hasContent) {
    getTextOutput().println();
  }
  hasContent=true;
  getTextOutput().println(header);
  for (int i=0; i < header.length(); i++) {
    getTextOutput().text("-");
  }
  getTextOutput().println();
}
