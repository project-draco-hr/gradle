{
  if (hasContent) {
    getFormatter().format("%n");
  }
  hasContent=true;
  getFormatter().format("%s%n",header);
  for (int i=0; i < header.length(); i++) {
    getFormatter().format("-");
  }
  getFormatter().format("%n");
}
