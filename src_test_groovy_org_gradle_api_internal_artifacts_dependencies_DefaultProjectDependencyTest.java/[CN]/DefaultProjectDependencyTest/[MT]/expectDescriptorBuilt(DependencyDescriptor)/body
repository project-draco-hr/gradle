{
  context.checking(new Expectations(){
{
      one(dependencyDescriptorFactoryMock).createFromProjectDependency(getParentModuleDescriptorMock(),projectDependency);
      will(returnValue(descriptor));
    }
  }
);
}
