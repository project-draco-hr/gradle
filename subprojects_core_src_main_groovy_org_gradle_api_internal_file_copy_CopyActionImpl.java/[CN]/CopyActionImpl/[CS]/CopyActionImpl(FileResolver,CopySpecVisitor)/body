{
  this.resolver=resolver;
  root=new CopySpecImpl(resolver);
  mainContent=root.addChild();
  this.visitor=new MappingCopySpecVisitor(new DuplicateHandlingCopySpecVisitor(new NormalizingCopySpecVisitor(visitor)),FileSystems.getDefault());
}
