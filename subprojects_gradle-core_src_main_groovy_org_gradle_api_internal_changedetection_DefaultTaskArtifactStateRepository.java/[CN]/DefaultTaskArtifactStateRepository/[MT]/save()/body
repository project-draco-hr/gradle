{
  try {
    ObjectOutputStream outStr=new ObjectOutputStream(new BufferedOutputStream(new FileOutputStream(getStateFile())));
    try {
      outStr.writeObject(outputs);
    }
  finally {
      outStr.close();
    }
  }
 catch (  IOException e) {
    throw new UncheckedIOException(e);
  }
  cache.update();
}
