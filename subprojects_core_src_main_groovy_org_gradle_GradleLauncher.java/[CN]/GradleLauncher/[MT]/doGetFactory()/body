{
  if (factory == null) {
    factory=ServiceRegistryBuilder.builder().displayName("Global services").parent(LoggingServiceRegistry.newProcessLogging()).parent(NativeServices.getInstance()).provider(new GlobalScopeServices()).build().get(GradleLauncherFactory.class);
  }
  return factory;
}
