{
  lock.lock();
  try {
    if (state != State.Running) {
      return;
    }
    try {
      outgoingQueue.requestStop();
      incomingQueue.requestStop();
    }
  finally {
      state=State.Stopping;
    }
  }
  finally {
    lock.unlock();
  }
}
