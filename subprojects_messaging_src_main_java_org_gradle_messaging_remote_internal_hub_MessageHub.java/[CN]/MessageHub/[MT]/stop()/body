{
  try {
    requestStop();
    workers.stop();
  }
  finally {
    lock.lock();
    try {
      outgoingChannels.clear();
      state=State.Stopped;
    }
  finally {
      lock.unlock();
    }
  }
}
