{
  try {
    for (    HandlerQueue handler : handlers) {
      handler.put(queue);
      handler.put(Collections.singleton(END_OF_STREAM));
    }
  }
  finally {
    queue.clear();
    handlers.clear();
  }
}
