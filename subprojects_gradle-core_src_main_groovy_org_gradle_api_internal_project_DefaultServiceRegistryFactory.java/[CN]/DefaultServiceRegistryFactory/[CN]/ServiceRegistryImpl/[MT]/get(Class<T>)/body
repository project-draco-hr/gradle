{
  for (  Service service : services) {
    T t=service.getService(serviceType);
    if (t != null) {
      return t;
    }
  }
  throw new IllegalArgumentException(String.format("No service of type %s available.",serviceType.getSimpleName()));
}
