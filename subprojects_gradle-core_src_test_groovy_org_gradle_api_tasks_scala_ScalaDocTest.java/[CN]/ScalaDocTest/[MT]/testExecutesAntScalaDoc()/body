{
  setUpMocksAndAttributes(scalaDoc);
  context.checking(new Expectations(){
{
      one(antScalaDocMock).execute(scalaDoc.getScalaSrcDirs(),scalaDoc.getScalaIncludes(),scalaDoc.getScalaExcludes(),scalaDoc.getDestinationDir(),scalaDoc.getClasspath(),scalaDoc.getScalaDocOptions());
    }
  }
);
  scalaDoc.execute();
}
