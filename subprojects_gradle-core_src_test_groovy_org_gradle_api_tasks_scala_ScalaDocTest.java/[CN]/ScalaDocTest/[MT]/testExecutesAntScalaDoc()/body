{
  setUpMocksAndAttributes(scalaDoc);
  context.checking(new Expectations(){
{
      one(antScalaDocMock).execute(scalaDoc.getSource(),scalaDoc.getDestinationDir(),scalaDoc.getClasspath(),scalaDoc.getScalaDocOptions());
    }
  }
);
  scalaDoc.execute();
}
