{
  if (status.isTenuredSpaceExhausted()) {
    return new DaemonExpirationResult(GRACEFUL_EXPIRE,"because JVM tenured space was exhausted");
  }
 else {
    return DaemonExpirationResult.NOT_TRIGGERED;
  }
}
