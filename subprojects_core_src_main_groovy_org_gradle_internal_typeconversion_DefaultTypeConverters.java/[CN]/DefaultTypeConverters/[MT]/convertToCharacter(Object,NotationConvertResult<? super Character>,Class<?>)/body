{
  String trimmed=notation.toString().trim();
  if (trimmed.length() != 1) {
    throw new TypeConversionException(String.format("Cannot coerce string value '%s' with length %d to type %s",trimmed,trimmed.length(),type.getSimpleName()));
  }
  result.converted(trimmed.charAt(0));
}
