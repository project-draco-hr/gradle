{
  List<String> allArgs=new ArrayList<String>();
  if (quiet) {
    allArgs.add("--quiet");
  }
  if (taskList) {
    allArgs.add("--tasks");
  }
  allArgs.addAll(args);
  allArgs.addAll(tasks);
  return allArgs;
}
