{
  resolverProvider=createResolverProvider();
  configurationContainer=new DefaultConfigurationContainer(ivyServiceStub,resolverProvider,projectDependenciesBuildInstruction);
  configuration=(DefaultConfiguration)configurationContainer.add(CONF_NAME);
}
