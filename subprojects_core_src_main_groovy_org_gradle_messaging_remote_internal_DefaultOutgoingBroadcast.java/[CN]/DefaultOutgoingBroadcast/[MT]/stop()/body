{
  lock.lock();
  try {
    new CompositeStoppable().add(protocolStack).add(channels.values()).add(connections.values()).stop();
  }
  finally {
    channels.clear();
    connections.clear();
    lock.unlock();
  }
}
