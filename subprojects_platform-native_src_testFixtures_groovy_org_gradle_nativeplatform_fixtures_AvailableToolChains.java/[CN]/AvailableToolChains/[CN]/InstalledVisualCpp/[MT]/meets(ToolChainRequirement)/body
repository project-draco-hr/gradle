{
switch (requirement) {
case Available:
case VisualCpp:
    return true;
case VisualCpp2012:
  return version.compareTo(VISUALSTUDIO_2012) >= 0;
case VisualCpp2013:
return version.compareTo(VISUALSTUDIO_2013) >= 0;
default :
return false;
}
}
