{
  return params.length == 1 && params[0] instanceof Closure || params.length == 1 && params[0] instanceof Class || params.length == 2 && params[0] instanceof Class && params[1] instanceof Closure;
}
