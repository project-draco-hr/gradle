{
  if (artifactPoms.get(name) != null) {
    throw new InvalidUserDataException(String.format("There can be only one artifact per pom. Artifact %s with file %s can't be assigned to pom %s",artifact.getName(),src.getAbsolutePath(),name));
  }
}
