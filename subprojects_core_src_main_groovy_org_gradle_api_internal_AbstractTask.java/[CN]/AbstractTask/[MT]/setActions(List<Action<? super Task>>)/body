{
  nagIfTaskNotInConfigurableState("Task.setActions(Actions<Task>)");
  nagUser=false;
  try {
    deleteAllActions();
    for (    Action<? super Task> action : actions) {
      doLast(action);
    }
  }
  finally {
    nagUser=true;
  }
}
