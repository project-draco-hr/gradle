{
  return new Ordering<ModelType<T>>(){
    @Override public int compare(    ModelType<T> left,    ModelType<T> right){
      return left.getDisplayName().compareTo(right.getDisplayName());
    }
  }
;
}
