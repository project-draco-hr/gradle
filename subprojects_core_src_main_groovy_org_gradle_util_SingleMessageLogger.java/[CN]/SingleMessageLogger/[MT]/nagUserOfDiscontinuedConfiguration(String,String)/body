{
  if (isEnabled() && CONFIGURATIONS.add(configurationName)) {
    LOGGER.warn(String.format("The %s configuration %s. %s",configurationName,getDeprecationMessage(),advice));
    logTraceIfNecessary();
  }
}
