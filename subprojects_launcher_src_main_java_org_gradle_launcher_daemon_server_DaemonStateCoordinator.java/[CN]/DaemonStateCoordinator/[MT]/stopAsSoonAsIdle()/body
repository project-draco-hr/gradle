{
  lock.lock();
  try {
    LOGGER.debug("Stop as soon as idle requested. The daemon is busy: " + isBusy());
    if (isBusy()) {
      onStopRequested.run();
      stoppingOrStopped=true;
    }
 else {
      stop();
    }
  }
  finally {
    lock.unlock();
  }
}
