{
  lock.lock();
  try {
    if (stoppingOrStopped) {
      return false;
    }
    stoppingOrStopped=true;
    onStopRequested.run();
    asyncStop.run();
    return true;
  }
  finally {
    lock.unlock();
  }
}
