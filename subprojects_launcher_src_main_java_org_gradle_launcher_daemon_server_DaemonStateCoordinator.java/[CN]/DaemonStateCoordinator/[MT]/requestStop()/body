{
  lock.lock();
  try {
    if (stoppingOrStopped) {
      return false;
    }
    stoppingOrStopped=true;
    onStopRequested.run();
    executor.execute(new Runnable(){
      public void run(){
        stop();
      }
    }
);
    return true;
  }
  finally {
    lock.unlock();
  }
}
