{
  lock.lock();
  try {
    try {
      beginStopping();
    }
  finally {
      executor.execute(new Runnable(){
        public void run(){
          stop();
        }
      }
);
    }
  }
  finally {
    lock.unlock();
  }
}
