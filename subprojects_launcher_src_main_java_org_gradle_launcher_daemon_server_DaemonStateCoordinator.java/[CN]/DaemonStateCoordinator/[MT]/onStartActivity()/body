{
  lock.lock();
  try {
    if (running) {
      throw new BusyException();
    }
    running=true;
    updateActivityTimestamp();
    activityStartHandler.run();
    condition.signalAll();
  }
  finally {
    lock.unlock();
  }
}
