{
  lock.lock();
  try {
    LOGGER.debug("Stop requested. The daemon is running a build: " + isBusy());
    stopped=true;
    onStop.run();
    condition.signalAll();
  }
  finally {
    lock.unlock();
  }
}
