{
  lock.lock();
  try {
    LOGGER.debug("Stop requested. The daemon is running a build: " + isBusy());
    if (!stopped) {
      onStopRequested.run();
    }
    stopped=true;
    onStop.run();
    condition.signalAll();
  }
  finally {
    lock.unlock();
  }
}
