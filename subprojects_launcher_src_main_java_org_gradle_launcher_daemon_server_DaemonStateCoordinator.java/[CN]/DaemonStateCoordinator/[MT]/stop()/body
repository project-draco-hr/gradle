{
  lock.lock();
  try {
    LOGGER.debug("Stop requested. The daemon is running a build: " + isBusy());
    try {
      beginStopping();
    }
  finally {
      finishStopping();
    }
  }
  finally {
    lock.unlock();
  }
}
