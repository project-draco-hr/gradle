{
  lock.lock();
  try {
    LOGGER.info("Stop requested. The daemon is running a build: " + running);
    stopped=true;
    stopHandler.run();
    condition.signalAll();
  }
  finally {
    lock.unlock();
  }
}
