{
  LinkedList<ProgressLogger> loggers=progressLoggers.get();
  progressLoggers.set(loggers);
  ProgressLogger logger=loggerFactory.newOperation(DependencyResolutionLogger.class).start(String.format("Resolve %s",dependencies),String.format("Resolving %s",dependencies));
  loggers.add(logger);
}
