{
  if (!HAS_CLASS_VALUE) {
    return false;
  }
  if (classpath == null) {
    return true;
  }
  for (  URI uri : classpath.getAsURIs()) {
    if (uri.getPath().contains("groovy")) {
      return true;
    }
  }
  return false;
}
