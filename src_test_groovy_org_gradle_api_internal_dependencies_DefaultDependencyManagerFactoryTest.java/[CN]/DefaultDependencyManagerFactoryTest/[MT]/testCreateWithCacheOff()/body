{
  DefaultDependencyManager dependencyManager=(DefaultDependencyManager)new DefaultDependencyManagerFactory(settingsFinderMock,CacheUsage.OFF).createDependencyManager(expectedProject);
  assertTrue(!expectedBuildResolverDir.isDirectory());
  checkCommon(expectedProject,dependencyManager);
}
