{
  if (value != null && value.length() == 0) {
    throw new InvalidMavenPublicationException(String.format("Invalid publication '%s': artifact %s cannot be an empty string. Use null instead.",publicationName,name));
  }
}
