{
  if (!projectIdentityValue.equals(pomFileValue)) {
    throw new InvalidMavenPublicationException(String.format("Invalid publication '%s': supplied %s does not match POM file (cannot edit %2$s directly in the POM file).",publicationName,name));
  }
}
