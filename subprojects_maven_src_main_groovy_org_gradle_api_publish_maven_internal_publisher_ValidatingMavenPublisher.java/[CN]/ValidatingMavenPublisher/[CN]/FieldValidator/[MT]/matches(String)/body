{
  if (!value.equals(expectedValue)) {
    throw new InvalidMavenPublicationException(publicationName,String.format("supplied %s does not match POM file (cannot edit %1$s directly in the POM file).",name));
  }
  return this;
}
