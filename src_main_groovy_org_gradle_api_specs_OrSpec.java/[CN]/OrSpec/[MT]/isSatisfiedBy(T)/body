{
  List<Spec<? super T>> specs=getSpecs();
  if (specs.isEmpty()) {
    return true;
  }
  for (  Spec<? super T> spec : specs) {
    if (spec.isSatisfiedBy(object)) {
      return true;
    }
  }
  return false;
}
