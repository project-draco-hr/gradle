{
  if (getAction() != null ? getAction().isEnabled() : isEnabled()) {
    Rectangle2D buttonRect=BorderlessImageButton.this.getBounds();
    if (buttonRect.contains(event.getPoint()))     BorderlessImageButton.this.setBackground(BorderlessUtility.ON_MOUSE_OVER_BACKGROUND);
  }
}
