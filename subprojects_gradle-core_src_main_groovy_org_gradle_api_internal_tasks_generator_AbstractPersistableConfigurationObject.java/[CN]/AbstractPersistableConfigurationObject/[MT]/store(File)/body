{
  try {
    Writer writer=new FileWriter(outputFile);
    try {
      store(writer);
    }
  finally {
      writer.close();
    }
  }
 catch (  IOException e) {
    throw UncheckedException.asUncheckedException(e);
  }
}
