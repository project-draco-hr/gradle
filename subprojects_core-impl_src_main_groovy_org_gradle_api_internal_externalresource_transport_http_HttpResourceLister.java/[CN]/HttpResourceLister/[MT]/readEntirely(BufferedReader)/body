{
  try {
    StringBuffer buf=new StringBuffer();
    String line=in.readLine();
    while (line != null) {
      buf.append(line);
      line=in.readLine();
      if (line != null) {
        buf.append("\n");
      }
    }
    return buf.toString();
  }
  finally {
    in.close();
  }
}
