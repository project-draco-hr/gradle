{
  lock.lock();
  try {
    if (running) {
      throw new BusyException();
    }
    running=true;
    activityListener.onStartActivity(this);
    condition.signalAll();
  }
  finally {
    lock.unlock();
  }
}
