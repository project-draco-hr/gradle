{
  if (this == o) {
    return true;
  }
  if (!(o instanceof DefaultSourceIncludes)) {
    return false;
  }
  DefaultSourceIncludes that=(DefaultSourceIncludes)o;
  return macroIncludes.equals(that.macroIncludes) && quotedIncludes.equals(that.quotedIncludes) && systemIncludes.equals(that.systemIncludes)&& allIncludes.equals(that.allIncludes);
}
