{
  lock.lock();
  cleanUp();
  try {
    long size=0;
    for (    Map<ModelType<?>,ModelSchema<?>> values : cache.values()) {
      size+=values.size();
    }
    return size;
  }
  finally {
    lock.unlock();
  }
}
