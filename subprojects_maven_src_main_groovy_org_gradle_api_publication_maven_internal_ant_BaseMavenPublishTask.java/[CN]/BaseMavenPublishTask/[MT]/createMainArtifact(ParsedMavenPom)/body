{
  return new DefaultArtifact(builder.getGroup(),builder.getArtifactId(),VersionRange.createFromVersion(builder.getVersion()),null,builder.getPackaging(),null,artifactHandler(builder.getPackaging()));
}
