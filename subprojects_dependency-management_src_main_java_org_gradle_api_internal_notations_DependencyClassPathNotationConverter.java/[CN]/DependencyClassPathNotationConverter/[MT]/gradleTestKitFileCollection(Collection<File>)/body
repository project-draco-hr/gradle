{
  List<File> gradleApi=classPathRegistry.getClassPath(GRADLE_API.name()).getAsFiles();
  testKitClasspath.removeAll(gradleApi);
  return (FileCollectionInternal)relocatedDepsJar(testKitClasspath,"gradleTestKit()","test-kit").plus(gradleApiFileCollection(gradleApi));
}
