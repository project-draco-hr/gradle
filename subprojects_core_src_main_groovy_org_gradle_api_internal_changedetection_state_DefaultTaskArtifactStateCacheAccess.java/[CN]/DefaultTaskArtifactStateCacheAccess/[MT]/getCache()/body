{
synchronized (lock) {
    if (cache == null) {
      cache=cacheRepository.cache("taskArtifacts").forObject(gradle).withDisplayName("task artifact state cache").withLockOptions(mode(FileLockManager.LockMode.Exclusive)).open();
    }
    return cache;
  }
}
