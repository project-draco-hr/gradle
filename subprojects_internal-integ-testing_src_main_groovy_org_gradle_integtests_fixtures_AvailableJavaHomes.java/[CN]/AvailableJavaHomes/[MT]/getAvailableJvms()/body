{
  return FluentIterable.from(getJvms()).transform(new Function<JvmInstallation,JavaInfo>(){
    @Override public JavaInfo apply(    @javax.annotation.Nullable JvmInstallation input){
      return Jvm.forHome(input.getJavaHome());
    }
  }
).toList();
}
