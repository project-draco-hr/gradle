{
  testFile("build.gradle").writelns("task a","task b","task c","taskVar = 'a'","def taskMethod() { 'b' }","c.dependsOn task(taskVar), task(taskMethod())");
  inTestDirectory().withTasks("c").run().assertTasksExecuted(":a",":b",":c");
}
