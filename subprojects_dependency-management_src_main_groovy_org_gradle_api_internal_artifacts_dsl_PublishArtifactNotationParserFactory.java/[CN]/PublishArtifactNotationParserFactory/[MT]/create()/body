{
  FileNotationParser fileParser=new FileNotationParser();
  return NotationParserBuilder.toType(PublishArtifact.class).parser(new ArchiveTaskNotationParser()).parser(new FileMapNotationParser(fileParser)).parser(fileParser).toComposite();
}
