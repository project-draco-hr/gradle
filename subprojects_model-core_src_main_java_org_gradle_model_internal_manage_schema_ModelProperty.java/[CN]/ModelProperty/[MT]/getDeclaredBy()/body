{
  return ImmutableSet.copyOf(Iterables.transform(declaredBy,new Function<ModelType.WeakRef<?>,ModelType<?>>(){
    @Override public ModelType<?> apply(    ModelType.WeakRef<?> input){
      return input.get();
    }
  }
));
}
