{
  if (accessor == null) {
    return false;
  }
  for (  Method method : accessor.getDeclaringMethods()) {
    if (method.isAnnotationPresent(Unmanaged.class)) {
      return true;
    }
  }
  return false;
}
