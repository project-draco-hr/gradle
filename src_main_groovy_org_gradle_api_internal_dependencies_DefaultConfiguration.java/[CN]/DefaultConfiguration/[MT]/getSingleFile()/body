{
  List<File> files=doResolve();
  if (files.size() != 1) {
    throw new IllegalStateException(String.format("Configuration '%s' does not resolve to a single file.",name));
  }
  return files.get(0);
}
