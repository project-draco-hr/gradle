{
  boolean shouldBeQuiet=options.hasOption(GRADLE_QUIET_OPTION) || options.getExtraArguments().contains("-" + GRADLE_QUIET_OPTION) || options.getExtraArguments().contains("--" + GRADLE_QUIET_DETAILED_OPTION);
  return new Logger(shouldBeQuiet);
}
