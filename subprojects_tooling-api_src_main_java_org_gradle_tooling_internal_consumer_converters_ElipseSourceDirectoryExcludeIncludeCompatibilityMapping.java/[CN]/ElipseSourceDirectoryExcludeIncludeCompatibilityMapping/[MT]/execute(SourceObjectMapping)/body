{
  Class<?> targetType=mapping.getTargetType();
  if (!supportsSourceDirectoryExcludeInclude && EclipseSourceDirectory.class.isAssignableFrom(targetType)) {
    mapping.mixIn(EclipseSourceDirectoryExcludeIncludeMixin.class);
  }
}
