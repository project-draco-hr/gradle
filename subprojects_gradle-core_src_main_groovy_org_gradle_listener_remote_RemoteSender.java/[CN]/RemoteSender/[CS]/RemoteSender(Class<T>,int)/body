{
  socket=new Socket((String)null,port);
  outstr=new BufferedOutputStream(socket.getOutputStream());
  source=type.cast(Proxy.newProxyInstance(type.getClassLoader(),new Class<?>[]{type},new SenderInvocationHandler()));
}
