{
  if (depth == 0) {
    buffer.append(node);
    return;
  }
  original.format("%n");
  StyledTextOutput output=new LinePrefixingStyledTextOutput(original,prefix + "    ");
  output.append(prefix);
  output.append("  - ");
  output.append(node);
}
