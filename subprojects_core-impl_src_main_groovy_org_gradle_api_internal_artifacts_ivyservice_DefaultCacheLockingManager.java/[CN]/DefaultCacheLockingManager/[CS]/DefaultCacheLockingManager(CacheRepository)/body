{
  cache=cacheRepository.store(CacheLayout.ROOT.getKey()).withDisplayName("artifact cache").withLayout(new CacheLayoutBuilder().withSharedCache().build()).withLockOptions(mode(FileLockManager.LockMode.None)).open();
  initMetaDataStoreDir();
}
