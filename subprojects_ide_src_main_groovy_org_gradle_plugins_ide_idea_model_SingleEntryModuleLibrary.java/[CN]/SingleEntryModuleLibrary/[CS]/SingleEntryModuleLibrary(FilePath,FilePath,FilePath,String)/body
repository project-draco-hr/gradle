{
  super(Arrays.asList(library),javadoc != null ? Arrays.asList(javadoc) : Lists.<Path>newArrayList(),source != null ? Arrays.asList(source) : Lists.<Path>newArrayList(),Sets.<JarDirectory>newLinkedHashSet(),scope);
}
