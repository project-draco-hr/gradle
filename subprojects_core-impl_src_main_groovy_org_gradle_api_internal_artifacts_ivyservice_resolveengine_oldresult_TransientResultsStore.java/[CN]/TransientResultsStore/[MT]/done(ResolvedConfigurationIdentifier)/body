{
  writeId(ROOT,id);
  LOG.debug("Closing results stream {}. Wrote root {}.",binaryStore,id);
  try {
    output.close();
  }
 catch (  IOException e) {
    throw throwAsUncheckedException(e);
  }
 finally {
    output=null;
  }
}
