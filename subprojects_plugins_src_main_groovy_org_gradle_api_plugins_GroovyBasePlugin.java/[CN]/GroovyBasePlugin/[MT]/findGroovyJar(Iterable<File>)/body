{
  if (classpath == null) {
    return null;
  }
  for (  File file : classpath) {
    Matcher matcher=GROOVY_JAR_PATTERN.matcher(file.getName());
    if (matcher.matches()) {
      return matcher;
    }
  }
  return null;
}
