{
  groovyConfiguration.getDependencies().whenObjectAdded(new Action<Dependency>(){
    public void execute(    Dependency dependency){
      DeprecationLogger.nagUserOfDiscontinuedConfiguration(GROOVY_CONFIGURATION_NAME,"Either rely on automatic configuration" + " of a task's 'groovyClasspath' based on the Groovy library on its 'classpath', or configure the task's 'groovyClasspath' directly.");
    }
  }
);
}
