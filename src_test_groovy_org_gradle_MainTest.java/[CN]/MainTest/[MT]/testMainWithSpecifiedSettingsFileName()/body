{
  checkMain("-c","somesettings");
  assertThat(actualStartParameter.getSettingsScriptSource(),instanceOf(StrictScriptSource.class));
  assertThat(actualStartParameter.getSettingsScriptSource().getSourceFile(),equalTo(new File("somesettings").getCanonicalFile()));
}
