{
  if (resolved) {
    return;
  }
  if (failure != null) {
    return;
  }
  try {
    resolveState.getRevision(resolveResult.getDescriptor());
    resolved=true;
  }
 catch (  ModuleVersionResolveException e) {
    failure=e;
  }
}
