{
  if (resolved) {
    return;
  }
  if (failure != null) {
    return;
  }
  try {
    resolveState.getRevision(resolver.getDescriptor());
    resolved=true;
  }
 catch (  ModuleVersionResolveException e) {
    failure=e;
  }
}
