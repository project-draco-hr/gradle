{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  FilePathCacheKey that=(FilePathCacheKey)o;
  return Objects.equal(this.file,that.file) && Objects.equal(this.url,that.url) && Objects.equal(this.canonicalUrl,that.canonicalUrl)&& Objects.equal(this.relPath,that.relPath);
}
