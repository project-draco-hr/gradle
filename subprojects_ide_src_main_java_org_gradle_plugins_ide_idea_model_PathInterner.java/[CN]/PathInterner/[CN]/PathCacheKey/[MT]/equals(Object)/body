{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  PathCacheKey that=(PathCacheKey)o;
  return Objects.equal(this.url,that.url) && Objects.equal(this.expandedUrl,that.expandedUrl) && Objects.equal(this.relPath,that.relPath);
}
