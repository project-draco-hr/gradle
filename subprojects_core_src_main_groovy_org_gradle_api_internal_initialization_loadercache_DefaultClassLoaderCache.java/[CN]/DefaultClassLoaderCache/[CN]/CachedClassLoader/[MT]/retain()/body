{
  if (refCount < 0) {
    refCount=1;
  }
 else   if (refCount == 0) {
    throw new IllegalStateException("Cannot retain already released classloader: " + classLoader);
  }
 else {
    ++refCount;
  }
  return this;
}
