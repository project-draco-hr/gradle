{
  if (refCount <= 0) {
    throw new IllegalStateException("Cannot release already released classloader: " + classLoader);
  }
  if (--refCount == 0) {
    if (parent != null) {
      parent.release();
    }
    bySpec.remove(spec);
  }
}
