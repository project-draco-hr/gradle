{
  if (metaMethod.getReturnType().equals(Void.TYPE)) {
    src.format("public void %s(%s v) { super.%s(v); %sSet = true; }%n",metaMethod.getName(),metaMethod.getParameterTypes()[0].getTheClass().getCanonicalName(),metaMethod.getName(),property.getName());
  }
 else {
    String returnTypeName=metaMethod.getReturnType().getCanonicalName();
    src.format("public %s %s(%s v) { %s r = super.%s(v); %sSet = true; return r; }%n",returnTypeName,metaMethod.getName(),metaMethod.getParameterTypes()[0].getTheClass().getCanonicalName(),returnTypeName,metaMethod.getName(),property.getName());
  }
}
