{
  if (obj == this) {
    return true;
  }
  if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  DefaultClassPath8 other=(DefaultClassPath8)obj;
  return files.equals(other.files);
}
