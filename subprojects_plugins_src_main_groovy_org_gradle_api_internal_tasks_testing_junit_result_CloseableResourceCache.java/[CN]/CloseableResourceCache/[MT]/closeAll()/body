{
  try {
    for (    Map.Entry<K,R> entry : cache.entrySet()) {
      close(entry.getKey(),entry.getValue());
    }
  }
 catch (  UncheckedIOException e) {
    cleanUpQuietly();
    throw e;
  }
 finally {
    cache.clear();
  }
}
