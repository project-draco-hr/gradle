{
  context.checking(new Expectations(){
{
      one(cacheRepository).getIndexedGlobalCache(with(equalTo("fileHashes")),with(equalTo(Collections.EMPTY_MAP)),with(notNullValue(Serializer.class)));
      will(returnValue(cache));
    }
  }
);
  hasher=new CachingHasher(delegate,cacheRepository);
}
