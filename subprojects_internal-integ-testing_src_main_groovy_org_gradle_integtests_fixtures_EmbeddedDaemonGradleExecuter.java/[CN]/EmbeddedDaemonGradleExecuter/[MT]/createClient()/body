{
  Factory<LoggingServiceRegistry> factory=new Factory<LoggingServiceRegistry>(){
    public LoggingServiceRegistry create(){
      return loggingServices;
    }
  }
;
  return new DaemonClient(new EmbeddedDaemonConnector(daemonRegistry,factory),clientMetaData(),new OutputEventListener(){
    public void onOutput(    OutputEvent event){
    }
  }
);
}
