{
  lock.lock();
  try {
    receivers--;
    if (receivers == 0 && onReceiversExhausted != null) {
      onReceiversExhausted.run();
    }
    condition.signalAll();
  }
  finally {
    lock.unlock();
  }
}
