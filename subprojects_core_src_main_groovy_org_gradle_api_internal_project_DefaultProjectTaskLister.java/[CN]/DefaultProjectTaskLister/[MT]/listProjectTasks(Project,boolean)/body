{
  ProjectInternal projectInternal=(ProjectInternal)project;
  TaskContainerInternal tasks=projectInternal.getTasks();
  tasks.actualize();
  return includeImplicit ? Sets.union(tasks,projectInternal.getImplicitTasks()) : tasks;
}
