{
  assertThat(factory.get(StandardOutputRedirector.class),instanceOf(DefaultStandardOutputRedirector.class));
  assertThat(factory.get(StandardOutputRedirector.class),sameInstance(factory.get(StandardOutputRedirector.class)));
}
