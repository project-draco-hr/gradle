{
  lifecycleLock.lock();
  try {
    if (!stopped) {
      LOGGER.debug("input forwarder stop requested");
      forwarder.stop();
      stopped=true;
    }
  }
  finally {
    lifecycleLock.unlock();
  }
}
