{
  lifecycleLock.lock();
  try {
    if (started) {
      LOGGER.debug("input forwarder stop requested");
      try {
        forwarder.stop();
      }
  finally {
        forwarder=null;
        started=false;
      }
    }
  }
  finally {
    lifecycleLock.unlock();
  }
}
