{
  Task a=createTask("a");
  Task b=task("b",a);
  Task c=task("c",b);
  dependsOn(a,c);
  try {
    taskExecuter.addTasks(toList(c));
    fail();
  }
 catch (  CircularReferenceException e) {
  }
}
