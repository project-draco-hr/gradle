{
  Task eclipse=project.createTask(ECLIPSE).dependsOn(configureEclipseProject(project),configureEclipseClasspath(project));
  project.createTask(WrapUtil.toMap("type",EclipseClean.class),ECLIPSE_CLEAN);
}
