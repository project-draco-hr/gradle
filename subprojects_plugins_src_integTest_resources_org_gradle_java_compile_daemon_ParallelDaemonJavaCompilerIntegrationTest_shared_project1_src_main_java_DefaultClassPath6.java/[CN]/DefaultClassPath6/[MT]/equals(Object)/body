{
  if (obj == this) {
    return true;
  }
  if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  DefaultClassPath6 other=(DefaultClassPath6)obj;
  return files.equals(other.files);
}
