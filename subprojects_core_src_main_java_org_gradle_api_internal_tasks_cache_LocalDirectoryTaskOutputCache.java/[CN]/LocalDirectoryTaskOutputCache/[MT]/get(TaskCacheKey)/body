{
  final File file=getFile(key.getHashCode());
  if (file.isFile()) {
    return new TaskOutputReader(){
      @Override public ByteSource read() throws IOException {
        return Files.asByteSource(file);
      }
    }
;
  }
  return null;
}
