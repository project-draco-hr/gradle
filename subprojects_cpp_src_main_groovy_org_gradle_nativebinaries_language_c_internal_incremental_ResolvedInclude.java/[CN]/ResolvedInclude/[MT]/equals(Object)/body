{
  if (this == o) {
    return true;
  }
  if (!(o instanceof ResolvedInclude)) {
    return false;
  }
  ResolvedInclude that=(ResolvedInclude)o;
  return include.equals(that.include) && (dependencyFile == null ? that.dependencyFile == null : dependencyFile.equals(that.dependencyFile));
}
