{
  return "{" + "dir: '" + getProjectDir().getAbsolutePath() + "'"+ ", tasks: '"+ GUtil.join(getTasks()," ")+ "'"+ ", arguments: '"+ GUtil.join(getArguments()," ")+ "'"+ ", gradleVersion: "+ getGradleVersion()+ "}";
}
