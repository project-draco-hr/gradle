{
  boolean didWork=false;
  for (  File file : fileResolver.resolveFiles(deletes)) {
    if (!file.exists()) {
      continue;
    }
    logger.debug("Deleting {}",file);
    didWork=true;
    doDelete(file);
  }
  return didWork;
}
