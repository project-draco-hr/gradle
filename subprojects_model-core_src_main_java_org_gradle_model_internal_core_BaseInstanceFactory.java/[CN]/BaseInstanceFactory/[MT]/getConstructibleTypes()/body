{
  return Sets.filter(getSupportedTypes(),new Predicate<ModelType<? extends T>>(){
    @Override public boolean apply(    ModelType<? extends T> registeredType){
      return !baseInterface.equals(registeredType);
    }
  }
);
}
