{
  StringBuilder builder=new StringBuilder(String.format("Cannot register %s for type '%s' because %s for this type was already registered",entity,publicType,entity));
  if (source != null) {
    builder.append(" by ");
    source.describeTo(builder);
  }
  return builder.toString();
}
