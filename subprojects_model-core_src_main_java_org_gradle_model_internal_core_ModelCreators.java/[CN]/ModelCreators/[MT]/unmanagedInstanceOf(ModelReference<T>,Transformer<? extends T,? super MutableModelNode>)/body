{
  return of(modelReference.getPath(),new Action<MutableModelNode>(){
    @Override public void execute(    MutableModelNode modelNode){
      T t=factory.transform(modelNode);
      modelNode.setPrivateData(modelReference.getType(),t);
    }
  }
).service(modelReference.getType().getConcreteClass().isAnnotationPresent(Service.class)).withProjection(UnmanagedModelProjection.of(modelReference.getType()));
}
