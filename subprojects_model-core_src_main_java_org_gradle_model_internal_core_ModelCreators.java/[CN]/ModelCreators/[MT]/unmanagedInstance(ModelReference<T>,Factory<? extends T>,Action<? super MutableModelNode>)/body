{
  @SuppressWarnings("unchecked") Action<MutableModelNode> initializers=Actions.composite(new Action<MutableModelNode>(){
    public void execute(    MutableModelNode modelNode){
      modelNode.setPrivateData(modelReference.getType(),factory.create());
    }
  }
,initializer);
  return of(modelReference.getPath(),initializers).withProjection(UnmanagedModelProjection.of(modelReference.getType()));
}
