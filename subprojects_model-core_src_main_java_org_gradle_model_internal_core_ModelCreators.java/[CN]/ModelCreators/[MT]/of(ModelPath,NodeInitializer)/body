{
  return of(path,new BiAction<MutableModelNode,List<ModelView<?>>>(){
    @Override public void execute(    MutableModelNode modelNode,    List<ModelView<?>> views){
      initializer.execute(modelNode,views);
    }
  }
).inputs(initializer.getInputs()).withProjections(initializer.getProjections());
}
