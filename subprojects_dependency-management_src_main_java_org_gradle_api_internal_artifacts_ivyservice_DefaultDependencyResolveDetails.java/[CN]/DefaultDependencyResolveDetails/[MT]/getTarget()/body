{
  ModuleComponentSelector realTarget=(ModuleComponentSelector)delegate.getTarget();
  return DefaultModuleVersionSelector.newSelector(realTarget.getGroup(),realTarget.getModule(),realTarget.getVersion());
}
