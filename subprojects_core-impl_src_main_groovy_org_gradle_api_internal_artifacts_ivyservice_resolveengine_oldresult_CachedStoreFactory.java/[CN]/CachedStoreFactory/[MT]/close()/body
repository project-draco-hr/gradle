{
  LOG.info("Resolved configuration cache: cache reads: " + stats.readsFromCache + ", disk reads: "+ stats.readsFromDisk+ " (avg: "+ prettyTime(stats.getDiskReadsAvgMs())+ ", total: "+ prettyTime(stats.diskReadsTotalMs.get())+ ")");
}
