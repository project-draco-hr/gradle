{
  LOG.info(displayName + " cache closed. Cache reads: " + stats.readsFromCache+ ", disk reads: "+ stats.readsFromDisk+ " (avg: "+ prettyTime(stats.getDiskReadsAvgMs())+ ", total: "+ prettyTime(stats.diskReadsTotalMs.get())+ ")");
}
