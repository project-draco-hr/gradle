{
  if (active) {
    throw new SecurityException("System.exit() detected. Gradle does not permit 3rd party or client builds to perform the vm exit.\n" + "The reason is that disappearing vm leads to problems that are very hard to diagnose, " + "especially when running with the gradle build daemon.\n");
  }
}
