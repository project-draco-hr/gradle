{
  if (!hasSrcBeenSet) {
    Object srcDirs=getSrcDirs();
    if (srcDirs != null) {
      from(srcDirs);
    }
  }
  if (!hasDestBeenSet) {
    File destDir=getDestinationDir();
    if (destDir != null) {
      into(destDir);
    }
  }
}
