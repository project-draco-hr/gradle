{
  return Lists.newArrayList(Iterables.transform(taskRequests,new Function<TaskExecutionRequest,String>(){
    public String apply(    TaskExecutionRequest input){
      return input.getTaskName();
    }
  }
));
}
