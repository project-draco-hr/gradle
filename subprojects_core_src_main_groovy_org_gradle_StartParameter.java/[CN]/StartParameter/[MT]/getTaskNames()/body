{
  return Lists.newArrayList(Iterables.transform(taskParameters,new Function<TaskExecutionRequest,String>(){
    public String apply(    TaskExecutionRequest input){
      return input.getTaskName();
    }
  }
));
}
