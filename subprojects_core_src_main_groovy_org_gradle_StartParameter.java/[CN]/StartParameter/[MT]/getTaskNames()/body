{
  return Lists.newArrayList(Iterables.transform(taskParameters,new Function<TaskParameter,String>(){
    public String apply(    TaskParameter input){
      return input.getTaskName();
    }
  }
));
}
