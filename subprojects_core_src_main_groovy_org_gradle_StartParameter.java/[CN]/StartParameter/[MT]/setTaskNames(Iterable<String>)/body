{
  this.taskParameters=Lists.newArrayList(Iterables.transform(taskNames != null ? taskNames : Collections.<String>emptyList(),new Function<String,TaskExecutionRequest>(){
    public TaskExecutionRequest apply(    String input){
      return new DefaultTaskExecutionRequest(input);
    }
  }
));
}
