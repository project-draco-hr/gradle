{
  this.taskParameters=Lists.newArrayList(Iterables.transform(taskNames != null ? taskNames : Collections.<String>emptyList(),new Function<String,TaskParameter>(){
    public TaskParameter apply(    String input){
      return new DefaultTaskParameter(input);
    }
  }
));
}
