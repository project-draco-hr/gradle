{
  ITaskFactory taskFactory=ModelViews.assertType(inputs.get(0),ModelType.of(ITaskFactory.class)).getInstance();
  NamedEntityInstantiator<Task> instantiator=new Instantiator(binary,taskFactory);
  DefaultCollectionBuilder<Task> collectionBuilder=new DefaultCollectionBuilder<Task>(ModelType.of(Task.class),binary.getTasks(),getDescriptor(),modelNode,instantiator){
    @Override protected <S extends Task>void onCreate(    String name,    ModelType<S> type){
      get(name);
    }
  }
;
  invoke(inputs,collectionBuilder,binary,taskFactory);
}
