{
  DomainObjectSet<T> matching=backingCollection.matching(new Spec<T>(){
    @Override public boolean isSatisfiedBy(    T element){
      return type.isInstance(element);
    }
  }
);
  return Cast.uncheckedCast(matching);
}
