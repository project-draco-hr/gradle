{
  for (  File file : classpath) {
    if (file.getName().matches(pattern)) {
      return file;
    }
  }
  throw new InvalidUserDataException(String.format("Cannot find a Jar matching pattern '%s' on Scala classpath %s. Please make sure it is available.",pattern,ImmutableList.copyOf(classpath)));
}
