{
  boolean isTaskMethod=call.getMethod() instanceof ConstantExpression || call.getMethod() instanceof GStringExpression;
  if (!isTaskMethod) {
    return false;
  }
  return call.getObjectExpression() instanceof VariableExpression && call.getObjectExpression().getText().equals("this");
}
