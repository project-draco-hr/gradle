{
  return new NamedDomainObjectSetBackedModelMap<T>(elementType,domainObjectSet,instantiator,new org.gradle.api.Namer<Object>(){
    public String determineName(    Object object){
      return Cast.cast(Named.class,object).getName();
    }
  }
);
}
