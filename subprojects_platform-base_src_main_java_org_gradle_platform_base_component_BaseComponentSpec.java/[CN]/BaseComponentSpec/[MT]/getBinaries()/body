{
  binaries.ensureUsable();
  return binaries.asMutable(ModelTypes.modelMap(BinarySpecInternal.PUBLIC_MODEL_TYPE),RuleContext.nest(modelNode.toString() + ".getBinaries()"),Collections.<ModelView<?>>emptyList()).getInstance();
}
