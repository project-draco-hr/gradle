{
  sources.ensureUsable();
  return sources.asWritable(ModelTypes.modelMap(LanguageSourceSet.class),RuleContext.nest(modelNode.toString() + ".getSources()"),Collections.<ModelView<?>>emptyList()).getInstance();
}
