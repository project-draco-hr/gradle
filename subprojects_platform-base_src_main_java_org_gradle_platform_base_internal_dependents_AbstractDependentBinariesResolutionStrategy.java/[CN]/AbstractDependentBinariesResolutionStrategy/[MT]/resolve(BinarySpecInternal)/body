{
  DependentBinariesResolvedResult root=new DefaultDependentBinariesResolvedResult(target.getId(),target.isBuildable(),isTestSuite(target),resolveDependents(target));
  return new DefaultDependentBinariesResolutionResult(root);
}
