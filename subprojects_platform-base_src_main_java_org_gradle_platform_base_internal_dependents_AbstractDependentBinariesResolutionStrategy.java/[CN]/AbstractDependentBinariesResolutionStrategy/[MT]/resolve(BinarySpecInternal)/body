{
  DependentBinariesResolvedResult root=new DefaultDependentBinariesResolvedResult(target.getId(),target.getProjectScopedName(),target.isBuildable(),isTestSuite(target),resolveDependents(target));
  return new DefaultDependentBinariesResolutionResult(root);
}
