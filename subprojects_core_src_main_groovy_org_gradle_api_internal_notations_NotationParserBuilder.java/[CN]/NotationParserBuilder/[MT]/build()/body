{
  Collection parsers=GUtil.flattenElements(new JustReturningParser(resultingType),this.notationParsers,new AlwaysThrowingParser(invalidNotationMessage));
  return new DefaultNotationParser<T>(parsers);
}
