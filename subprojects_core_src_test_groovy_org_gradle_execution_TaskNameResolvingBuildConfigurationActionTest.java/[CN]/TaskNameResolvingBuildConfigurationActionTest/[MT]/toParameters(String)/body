{
  return Lists.newArrayList(Iterables.transform(Lists.newArrayList(parameters),new Function<String,TaskParameter>(){
    @Nullable public TaskParameter apply(    @Nullable String input){
      return new DefaultTaskParameter(input);
    }
  }
));
}
