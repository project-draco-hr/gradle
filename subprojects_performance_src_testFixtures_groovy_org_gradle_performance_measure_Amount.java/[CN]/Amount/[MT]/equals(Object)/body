{
  if (obj == this) {
    return true;
  }
  if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  @SuppressWarnings("unchecked") Amount<Q> other=(Amount)obj;
  return compareTo(other) == 0;
}
