{
  if (isOpen) {
    delegateCache.open();
    T returnValue=factory.create();
    delegateCache.close();
    return returnValue;
  }
 else {
    throw new CacheOpenException("Cannot run operation on not opened cache");
  }
}
