{
  if (isOpen) {
    delegateCache.open();
    try {
      return factory.create();
    }
  finally {
      delegateCache.close();
    }
  }
 else {
    throw new CacheOpenException("Cannot run operation on not opened cache");
  }
}
