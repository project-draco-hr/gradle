{
  try {
    return state.failure.toString();
  }
 catch (  Throwable t) {
    return String.format("Could not determine failure message for exception of type %s: %s",state.failure.getClass().getName(),t);
  }
}
