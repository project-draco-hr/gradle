{
  Jar jar=artifact.getTasks().getJar();
  PublishArtifact publishArtifact=new ArchivePublishArtifact(jar);
  return new PublishArtifactLocalArtifactMetaData(libraryId,libraryId.getDisplayName(),libraryId.getDisplayName(),publishArtifact);
}
