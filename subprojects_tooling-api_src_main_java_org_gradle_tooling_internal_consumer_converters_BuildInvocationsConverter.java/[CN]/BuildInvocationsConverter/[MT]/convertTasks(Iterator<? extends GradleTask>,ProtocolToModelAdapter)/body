{
  return Lists.newArrayList(Iterators.transform(tasks,new Function<GradleTask,Task>(){
    public Task apply(    GradleTask task){
      return adapter.adapt(Task.class,new DefaultGradleTask().setName(task.getName()).setPath(task.getPath()).setDescription(task.getDescription()));
    }
  }
));
}
