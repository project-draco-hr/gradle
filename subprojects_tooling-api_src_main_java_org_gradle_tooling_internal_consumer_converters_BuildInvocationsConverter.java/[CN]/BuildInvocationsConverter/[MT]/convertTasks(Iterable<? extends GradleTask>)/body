{
  List<DefaultGradleTask> result=Lists.newArrayList();
  for (  GradleTask task : tasks) {
    result.add(new DefaultGradleTask().setName(task.getName()).setPath(task.getPath()).setDescription(task.getDescription()));
  }
  return result;
}
