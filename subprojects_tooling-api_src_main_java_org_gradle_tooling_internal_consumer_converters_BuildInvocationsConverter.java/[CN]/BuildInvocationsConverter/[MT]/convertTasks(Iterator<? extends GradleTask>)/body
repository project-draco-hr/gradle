{
  return Lists.newArrayList(Iterators.transform(tasks,new Function<GradleTask,DefaultGradleTask>(){
    public DefaultGradleTask apply(    GradleTask task){
      return new DefaultGradleTask().setName(task.getName()).setPath(task.getPath()).setDescription(task.getDescription());
    }
  }
));
}
