{
  Stoppable stoppable;
  lock.lock();
  try {
    if (receiveState != State.Stopped || dispatchState != State.Stopped || connection == null) {
      return;
    }
    stoppable=connection;
    connection=null;
  }
  finally {
    lock.unlock();
  }
  stoppable.stop();
}
