{
  Stoppable stoppable;
  lock.lock();
  try {
    if (state != State.Stopped || connection == null) {
      return;
    }
    stoppable=connection;
    connection=null;
  }
  finally {
    lock.unlock();
  }
  stoppable.stop();
}
