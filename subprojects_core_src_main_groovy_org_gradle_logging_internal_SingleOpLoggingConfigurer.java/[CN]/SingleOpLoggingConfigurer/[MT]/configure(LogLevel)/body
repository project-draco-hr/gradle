{
  if (configured) {
    return;
  }
  sync.synchronize(new Operation(){
    public void execute(){
      if (configured) {
        return;
      }
      configured=true;
      delegate.configure(logLevel);
    }
  }
);
}
