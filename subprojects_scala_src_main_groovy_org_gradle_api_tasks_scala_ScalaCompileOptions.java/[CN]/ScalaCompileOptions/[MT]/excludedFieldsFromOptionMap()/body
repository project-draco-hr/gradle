{
  ImmutableList.Builder<String> builder=new ImmutableList.Builder<String>().add("useCompileDaemon","forkOptions","useAnt","compilerCacheFile");
  if (!optimize) {
    builder.add("optimize");
  }
  return builder.build();
}
