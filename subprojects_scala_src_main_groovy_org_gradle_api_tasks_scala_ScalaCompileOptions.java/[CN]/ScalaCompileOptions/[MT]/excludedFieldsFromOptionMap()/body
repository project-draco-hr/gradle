{
  ImmutableList.Builder<String> builder=new ImmutableList.Builder<String>().add("useCompileDaemon","forkOptions","incremental","incrementalCacheFile");
  if (!optimize) {
    builder.add("optimize");
  }
  return builder.build();
}
