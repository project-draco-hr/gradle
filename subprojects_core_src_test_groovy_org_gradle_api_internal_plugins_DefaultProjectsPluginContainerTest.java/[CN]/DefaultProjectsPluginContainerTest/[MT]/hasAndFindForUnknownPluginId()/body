{
  context.checking(new Expectations(){
{
      allowing(pluginRegistryStub).getTypeForId("unknown");
      will(throwException(new UnknownPluginException("unknown")));
    }
  }
);
  assertThat(container.hasPlugin("unknown"),equalTo(false));
  assertThat(container.findPlugin("unknown"),nullValue());
}
