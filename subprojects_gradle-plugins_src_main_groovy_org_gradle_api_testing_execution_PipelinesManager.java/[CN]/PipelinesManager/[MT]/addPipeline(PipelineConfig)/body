{
  lock.lock();
  try {
    final QueueingPipeline pipeline=pipelineFactory.createPipeline(this,pipelineIdSequence++,pipelineConfig,testReportProcessor);
    orderedPipelines.add(pipeline);
    notStopped.add(pipeline);
    return pipeline;
  }
  finally {
    lock.unlock();
  }
}
