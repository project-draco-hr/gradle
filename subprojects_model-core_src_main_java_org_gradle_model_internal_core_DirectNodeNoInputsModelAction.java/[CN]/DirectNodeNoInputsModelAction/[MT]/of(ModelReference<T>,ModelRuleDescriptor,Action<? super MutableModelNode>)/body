{
  return new DirectNodeNoInputsModelAction<T>(reference,descriptor,new BiAction<MutableModelNode,T>(){
    @Override public void execute(    MutableModelNode modelNode,    T t){
      action.execute(modelNode);
    }
  }
);
}
