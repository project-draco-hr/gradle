{
  executor=executorFactory.create(String.format("Dispatch %s",targetObject));
  exceptionListener=new ExceptionTrackingListener(Logging.getLogger(ActorImpl.class));
  dispatch=new AsyncDispatch<MethodInvocation>(executor,new ExceptionTrackingDispatch<MethodInvocation>(new ReflectionDispatch(targetObject),exceptionListener));
}
