{
  return new DefaultDeployerFactory(new DefaultMavenFactory(),loggingManagerFactory,fileResolver,new MavenPomMetaInfoProvider(){
    public File getMavenPomDir(){
      return publication.getPomDir();
    }
  }
,getProject().getConfigurations(),new DefaultConf2ScopeMappingContainer());
}
