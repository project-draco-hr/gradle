{
  super(project,name);
  setDagNeutral(true);
  doFirst(new TaskAction(){
    public void execute(    Task task){
      try {
        generate();
      }
 catch (      IOException e) {
        throw new GradleException(e);
      }
    }
  }
);
}
