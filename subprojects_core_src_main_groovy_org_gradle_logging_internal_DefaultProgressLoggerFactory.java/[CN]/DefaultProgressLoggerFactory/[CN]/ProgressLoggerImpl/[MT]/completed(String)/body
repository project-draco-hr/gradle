{
  assertStarted();
  assertNotCompleted();
  state=State.completed;
  listener.completed(new ProgressCompleteEvent(hierarchy.completeCurrentOperation(),timeProvider.getCurrentTime(),category,description,toStatus(status)));
}
