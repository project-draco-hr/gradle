{
  lock.lock();
  try {
    if (!stateIn(ExecHandleState.FAILED)) {
      throw new IllegalStateException("not in failed state!");
    }
    return failureCause;
  }
  finally {
    lock.unlock();
  }
}
