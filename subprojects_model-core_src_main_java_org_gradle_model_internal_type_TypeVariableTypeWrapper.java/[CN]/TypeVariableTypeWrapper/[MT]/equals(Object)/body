{
  if (!(o instanceof TypeVariable)) {
    return false;
  }
 else {
    TypeVariable<?> var2=(TypeVariable<?>)o;
    return Objects.equal(this.getName(),var2.getName()) && Arrays.equals(this.getBounds(),var2.getBounds());
  }
}
