{
  List<URL> urls=new ArrayList<URL>();
  for (  URI uri : uris) {
    try {
      urls.add(uri.toURL());
    }
 catch (    MalformedURLException e) {
      throw new UncheckedIOException(e);
    }
  }
  return urls;
}
