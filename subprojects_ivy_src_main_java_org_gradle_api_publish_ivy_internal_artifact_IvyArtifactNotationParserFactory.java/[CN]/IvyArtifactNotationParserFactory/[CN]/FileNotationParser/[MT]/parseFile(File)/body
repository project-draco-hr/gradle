{
  ArtifactFile artifactFile=new ArtifactFile(file,version);
  return instantiator.newInstance(DefaultIvyArtifact.class,file,null,artifactFile.getExtension(),artifactFile.getExtension(),artifactFile.getClassifier());
}
