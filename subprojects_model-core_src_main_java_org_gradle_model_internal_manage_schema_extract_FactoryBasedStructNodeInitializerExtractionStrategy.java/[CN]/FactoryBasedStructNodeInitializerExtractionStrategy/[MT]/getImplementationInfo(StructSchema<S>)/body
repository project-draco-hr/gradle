{
  ModelType<S> publicType=schema.getType();
  return schema instanceof ManagedImplSchema ? instanceFactory.getManagedSubtypeImplementationInfo(publicType) : instanceFactory.getImplementationInfo(publicType);
}
