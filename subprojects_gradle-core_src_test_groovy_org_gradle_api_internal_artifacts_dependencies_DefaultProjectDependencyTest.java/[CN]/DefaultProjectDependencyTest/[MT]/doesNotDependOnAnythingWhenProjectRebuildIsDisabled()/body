{
  DefaultProjectDependency dependency=new DefaultProjectDependency(targetProjectStub,new ProjectDependenciesBuildInstruction(null));
  assertThat(dependency.getBuildDependencies().getDependencies(null),isEmpty());
}
