{
  lock.lock();
  try {
    if (running) {
      throw new BusyException();
    }
    running=true;
    condition.signalAll();
    activityListener.onStart();
  }
  finally {
    lock.unlock();
  }
}
