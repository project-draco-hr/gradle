{
  StringBuilder sb=new StringBuilder();
  sb.append(getName());
  Type[] bounds=getBounds();
  if (bounds.length > 0) {
    sb.append(" extends ");
    sb.append(Joiner.on(" & ").join(bounds));
  }
  return sb.toString();
}
