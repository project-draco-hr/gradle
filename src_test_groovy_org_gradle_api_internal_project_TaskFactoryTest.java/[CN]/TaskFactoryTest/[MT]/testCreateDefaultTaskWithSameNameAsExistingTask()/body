{
  tasks.put("name",new DefaultTask(testProject,"name"));
  try {
    taskFactory.createTask(testProject,tasks,empyArgMap,"name");
    fail();
  }
 catch (  InvalidUserDataException e) {
    assertEquals("Cannot create task with name 'name' as a task with that name already exists.",e.getMessage());
  }
}
