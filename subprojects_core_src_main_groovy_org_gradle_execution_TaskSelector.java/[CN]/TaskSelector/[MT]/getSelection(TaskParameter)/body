{
  ProjectInternal project=taskParameter.getProjectPath() != null ? gradle.getRootProject().findProject(taskParameter.getProjectPath()) : gradle.getDefaultProject();
  return getSelection(taskParameter.getTaskName(),project);
}
