{
  return new BaseMavenInstaller(pomFilterContainerMock,artifactPomContainerMock,loggingManagerMock){
    @Override protected CustomInstallTask createTask(){
      return installTaskMock;
    }
  }
;
}
