{
  lock.lock();
  try {
    assertNotStopped();
    if (communicator == null) {
      communicator=new FileLockCommunicator();
    }
    return communicator.getPort();
  }
  finally {
    lock.unlock();
  }
}
