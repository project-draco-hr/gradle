{
  DefaultMavenArtifact artifact=instantiator.newInstance(DefaultMavenArtifact.class,archiveTask.getArchivePath(),GUtil.elvis(archiveTask.getExtension(),null),GUtil.elvis(archiveTask.getClassifier(),null));
  artifact.builtBy(archiveTask);
  return artifact;
}
