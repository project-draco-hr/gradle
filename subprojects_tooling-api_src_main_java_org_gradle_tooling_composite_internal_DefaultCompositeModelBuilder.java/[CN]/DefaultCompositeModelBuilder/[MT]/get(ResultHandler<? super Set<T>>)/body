{
  delegate.get(new ResultHandler<SetOfEclipseProjects>(){
    @Override public void onComplete(    SetOfEclipseProjects result){
      handler.onComplete((Set<T>)result.getResult());
    }
    @Override public void onFailure(    GradleConnectionException failure){
      handler.onFailure(failure);
    }
  }
);
}
