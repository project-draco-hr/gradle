{
  if (obj == this) {
    return true;
  }
  if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  DefaultClassPath4 other=(DefaultClassPath4)obj;
  return files.equals(other.files);
}
