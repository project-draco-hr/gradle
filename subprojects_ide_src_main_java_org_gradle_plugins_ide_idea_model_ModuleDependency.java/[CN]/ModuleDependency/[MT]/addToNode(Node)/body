{
  Map<String,Object> attributes=Maps.newHashMap(ImmutableMap.<String,Object>builder().put("type","module").put("module-name",name).putAll(getAttributeMapForScopeAndExported()).build());
  parentNode.appendNode("orderEntry",attributes);
}
