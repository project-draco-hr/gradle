{
  if (cache != null) {
    try {
      fileAccess.updateFile(new Runnable(){
        public void run(){
          cache.close();
        }
      }
);
    }
  finally {
      cache=null;
    }
  }
}
