{
  if (cache != null) {
    try {
      fileAccess.writeToFile(new Runnable(){
        public void run(){
          cache.close();
        }
      }
);
    }
  finally {
      cache=null;
    }
  }
}
