{
  if (result == null) {
    result=new DefaultResolvedDependency(descriptor.getModuleRevisionId().getOrganisation(),descriptor.getModuleRevisionId().getName(),descriptor.getModuleRevisionId().getRevision(),configurationName);
  }
  return result;
}
