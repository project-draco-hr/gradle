{
  Closeable c=openFiles.remove(file);
  if (c != null) {
    try {
      c.close();
    }
 catch (    IOException e) {
      cleanUpQuietly();
      throw new UncheckedIOException("Problems closing file: " + file,e);
    }
  }
}
