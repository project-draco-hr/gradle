{
  boolean dagNeutral=true;
  for (  Task task : tasks) {
    if (!task.getExecuted()) {
      executeTask(task);
      if (dagNeutral) {
        dagNeutral=task.isDagNeutral();
      }
    }
  }
  return dagNeutral;
}
