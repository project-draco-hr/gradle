{
  if (e instanceof StrictConflictException) {
    throw new GradleException("Your dependencies exhibit a version conflict. " + "The conflict resolution strategy is set to: " + conflictStrategy + ". Details: "+ e.getMessage(),e);
  }
}
