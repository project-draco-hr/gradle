{
  lock.lock();
  try {
    new CompositeStoppable(protocolStack).add(connections).add(asyncReceive,demultiplex,executor).stop();
  }
  finally {
    channels.clear();
    connections.clear();
    lock.unlock();
  }
}
