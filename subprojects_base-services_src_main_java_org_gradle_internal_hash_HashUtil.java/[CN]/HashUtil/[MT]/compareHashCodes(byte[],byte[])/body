{
  int result;
  int len=a.length;
  result=len - b.length;
  if (result == 0) {
    for (int idx=0; idx < len; idx++) {
      result=a[idx] - b[idx];
      if (result != 0) {
        break;
      }
    }
  }
  return result;
}
