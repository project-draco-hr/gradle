{
  String rev=findUniqueSnapshotVersion(moduleRevisionId);
  if (rev != null) {
    String pattern=getWholePattern().replaceFirst("\\-\\[revision\\]","-" + rev);
    return findResourceUsingPattern(moduleRevisionId,pattern,artifact,getDefaultRMDParser(artifact.getModuleRevisionId().getModuleId()),date);
  }
  return null;
}
