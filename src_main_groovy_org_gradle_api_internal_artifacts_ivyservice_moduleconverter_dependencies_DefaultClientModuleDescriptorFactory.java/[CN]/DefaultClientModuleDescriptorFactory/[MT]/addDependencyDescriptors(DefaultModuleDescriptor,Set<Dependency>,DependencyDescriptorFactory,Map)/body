{
  for (  Dependency dependency : dependencies) {
    moduleDescriptor.addDependency(dependencyDescriptorFactory.createDependencyDescriptor(Dependency.DEFAULT_CONFIGURATION,moduleDescriptor,dependency,clientModuleRegistry));
  }
}
