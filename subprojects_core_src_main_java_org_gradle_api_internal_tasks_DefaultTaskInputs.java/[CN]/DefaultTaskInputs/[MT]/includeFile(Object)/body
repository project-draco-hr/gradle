{
  return taskMutator.mutate("TaskInputs.includeFile(Object)",new Callable<TaskFileInputPropertySpec>(){
    @Override public TaskFileInputPropertySpec call() throws Exception {
      return addSpec(path);
    }
  }
);
}
