{
  return taskMutator.mutate("TaskInputs.includeFile(Object)",new Callable<TaskInputFilePropertySpec>(){
    @Override public TaskInputFilePropertySpec call() throws Exception {
      return addSpec(path);
    }
  }
);
}
