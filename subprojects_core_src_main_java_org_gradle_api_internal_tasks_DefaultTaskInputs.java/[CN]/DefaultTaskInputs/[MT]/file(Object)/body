{
  return taskMutator.mutate("TaskInputs.file(Object)",new Callable<TaskInputFilePropertyBuilder>(){
    @Override public TaskInputFilePropertyBuilder call(){
      return addSpec(path);
    }
  }
);
}
