{
  return taskMutator.mutate("TaskInputs.files(Object...)",new Callable<TaskInputFilePropertySpec>(){
    @Override public TaskInputFilePropertySpec call(){
      return addSpec(paths);
    }
  }
);
}
