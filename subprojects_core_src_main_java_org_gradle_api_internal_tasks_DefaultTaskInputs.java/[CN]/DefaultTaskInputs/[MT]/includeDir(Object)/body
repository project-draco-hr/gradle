{
  return taskMutator.mutate("TaskInputs.includeDir(Object)",new Callable<TaskFileInputPropertySpec>(){
    @Override public TaskFileInputPropertySpec call(){
      return addSpec(taskName,resolver.resolveFilesAsTree(path));
    }
  }
);
}
