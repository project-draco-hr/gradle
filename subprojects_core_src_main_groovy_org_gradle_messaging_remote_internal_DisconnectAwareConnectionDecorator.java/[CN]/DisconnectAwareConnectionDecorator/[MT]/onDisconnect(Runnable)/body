{
  actionLock.lock();
  try {
    Runnable previous=disconnectAction;
    this.disconnectAction=disconnectAction;
    actionSetLatch.countDown();
    return previous;
  }
  finally {
    actionLock.unlock();
  }
}
