{
  if (binaries.isEmpty()) {
    return true;
  }
  ProjectNativeBinaryInternal prototype=binaries.iterator().next();
  return prototype.getSource().size() == candidate.getSource().size() && prototype.getSource().containsAll(candidate.getSource());
}
