{
  return Iterables.filter(methods,new Predicate<Method>(){
    @Override public boolean apply(    Method method){
      return !method.isSynthetic() && !IGNORED_METHODS.contains(METHOD_EQUIVALENCE.wrap(method)) && !ignoreMethod(method);
    }
  }
);
}
