{
  if (type.isInstance(delegate)) {
    return type.cast(delegate);
  }
 else {
    throw new IllegalArgumentException(String.format("Cannot create DslObject for '%s' as it does not implement '%s' (it is not a DSL object)",delegate,type.getName()));
  }
}
