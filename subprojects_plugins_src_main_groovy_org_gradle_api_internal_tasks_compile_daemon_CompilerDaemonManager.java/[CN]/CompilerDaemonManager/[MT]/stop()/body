{
  if (client == null) {
    return;
  }
  LOGGER.info("Stopping Gradle compiler daemon.");
  client.stop();
  client=null;
  process.waitForStop();
  process=null;
  LOGGER.info("Gradle compiler daemon stopped.");
}
