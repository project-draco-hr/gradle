{
  return new S3TransportBuilder().awsCredentials((AwsCredentials)credentials).cacheLockingManager(cacheLockingManager).cachedExternalResourceIndex(cachedExternalResourceIndex).name(name).progressLoggerFactory(progressLoggerFactory).temporaryFileProvider(temporaryFileProvider).timeProvider(timeProvider).build();
}
