{
  lock.lock();
  try {
    depth--;
    if (!wasStarted) {
      started=false;
      if (depth <= 0) {
        owner=null;
        operationDisplayName=null;
        condition.signalAll();
      }
    }
  }
  finally {
    lock.unlock();
  }
}
