{
  Element element=document.createElement(tokenType == JavadocLexer.TokenType.StartElement ? "UNHANDLED-ELEMENT" : "UNHANDLED-TAG");
  element.appendChild(document.createTextNode(String.format("<%s>",tag)));
  nodes.push(tag,element);
  return true;
}
