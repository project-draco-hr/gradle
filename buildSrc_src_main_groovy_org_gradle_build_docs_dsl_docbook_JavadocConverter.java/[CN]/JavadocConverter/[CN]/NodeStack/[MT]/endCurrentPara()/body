{
  if (stack.isEmpty() || !stack.getFirst().getNodeName().equals("para")) {
    return;
  }
  Element para=stack.removeFirst();
  tags.removeFirst();
  if (stack.isEmpty()) {
    appendToResult(para);
  }
}
