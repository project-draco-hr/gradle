{
  try {
    char[] buffer=new char[2048];
    int read=content.read(buffer);
    if (read < 0) {
      return;
    }
    writer.writeCDATA(buffer,0,read);
  }
  finally {
    content.close();
  }
}
