{
  if (StringUtils.isEmpty(execCommand))   throw new IllegalStateException("execCommand == null!");
  return new DefaultExecHandle(execDirectory,execCommand,arguments,environment,keepWaitingTimeout,standardOutputHandle,errorOutputHandle,listeners);
}
