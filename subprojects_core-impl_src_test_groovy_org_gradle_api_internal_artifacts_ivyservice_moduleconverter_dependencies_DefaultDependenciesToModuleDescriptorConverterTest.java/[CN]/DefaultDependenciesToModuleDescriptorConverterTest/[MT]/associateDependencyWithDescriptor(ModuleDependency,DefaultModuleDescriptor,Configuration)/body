{
  context.checking(new Expectations(){
{
      allowing(dependencyDescriptorFactoryStub).createDependencyDescriptor(with(equal(configuration.getName())),with(equal(parent)),with(sameInstance(dependency)));
    }
  }
);
}
