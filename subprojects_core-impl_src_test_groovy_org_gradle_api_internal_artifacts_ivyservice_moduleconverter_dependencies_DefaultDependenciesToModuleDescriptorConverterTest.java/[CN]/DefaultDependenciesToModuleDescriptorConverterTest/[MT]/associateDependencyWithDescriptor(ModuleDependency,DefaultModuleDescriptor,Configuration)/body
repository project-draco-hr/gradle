{
  context.checking(new Expectations(){
{
      allowing(dependencyDescriptorFactoryStub).addDependencyDescriptor(with(sameInstance(configuration)),with(equal(parent)),with(sameInstance(dependency)));
    }
  }
);
}
