{
  if (startParameter.getSettingsScriptSource() != null) {
    return new SettingsLocation(startParameter.getCurrentDir(),startParameter.getSettingsScriptSource());
  }
 else {
    return finder.find(startParameter);
  }
}
