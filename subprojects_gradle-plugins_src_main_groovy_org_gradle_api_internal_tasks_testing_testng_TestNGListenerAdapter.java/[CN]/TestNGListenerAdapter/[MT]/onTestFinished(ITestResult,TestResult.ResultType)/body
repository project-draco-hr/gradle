{
  TestResult result=new DefaultTestResult(resultType,iTestResult.getThrowable(),iTestResult.getStartMillis(),iTestResult.getEndMillis());
  TestInternal testInternal;
synchronized (lock) {
    testInternal=tests.remove(iTestResult.getName());
  }
  resultProcessor.completed(testInternal,result);
}
