{
  Object testId;
synchronized (lock) {
    testId=tests.remove(iTestResult.getName());
  }
  resultProcessor.completed(testId,new TestCompleteEvent(iTestResult.getEndMillis(),resultType,resultType == TestResult.ResultType.FAILURE ? iTestResult.getThrowable() : null));
}
