{
  CacheFactory factory=new LazyOpenCacheFactory(get(CacheFactory.class));
  return new DefaultCacheRepository(startParameter.getGradleUserHomeDir(),startParameter.getProjectCacheDir(),startParameter.getCacheUsage(),factory);
}
