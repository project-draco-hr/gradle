{
  Instantiator instantiator=parent.get(Instantiator.class);
  ResolverFactory resolverFactory=new DefaultResolverFactory(parent.getFactory(LoggingManagerInternal.class),parent.get(MavenFactory.class),new DefaultLocalMavenCacheLocator(),fileResolver,instantiator);
  return instantiator.newInstance(DefaultRepositoryHandler.class,resolverFactory,fileResolver,instantiator);
}
