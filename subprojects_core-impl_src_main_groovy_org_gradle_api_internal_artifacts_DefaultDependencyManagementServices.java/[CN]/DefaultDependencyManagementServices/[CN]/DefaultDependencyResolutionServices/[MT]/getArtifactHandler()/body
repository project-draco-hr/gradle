{
  if (artifactHandler == null) {
    artifactHandler=new DefaultArtifactHandler(getConfigurationContainer(),new DefaultPublishArtifactFactory(get(Instantiator.class),dependencyMetaDataProvider));
  }
  return artifactHandler;
}
