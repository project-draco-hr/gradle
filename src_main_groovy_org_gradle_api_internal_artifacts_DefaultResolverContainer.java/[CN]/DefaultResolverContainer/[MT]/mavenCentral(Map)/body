{
  List<String> urls=createStringifiedListFromMapArg(args,"urls");
  return add(createMavenRepoResolver(getNameFromMap(args,DEFAULT_MAVEN_CENTRAL_REPO_NAME),MAVEN_CENTRAL_URL,urls == null ? WrapUtil.<String>toArray() : urls.toArray(new String[urls.size()])));
}
