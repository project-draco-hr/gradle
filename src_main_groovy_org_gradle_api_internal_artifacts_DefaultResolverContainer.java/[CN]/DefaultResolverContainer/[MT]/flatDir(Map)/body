{
  Object[] rootDirs=getFlatDirRootDirs(args);
  FileSystemResolver resolver=createFlatDirResolver(getNameFromMap(args,HashUtil.createHash(GUtil.join(rootDirs,""))),rootDirs);
  return (FileSystemResolver)add(resolver);
}
