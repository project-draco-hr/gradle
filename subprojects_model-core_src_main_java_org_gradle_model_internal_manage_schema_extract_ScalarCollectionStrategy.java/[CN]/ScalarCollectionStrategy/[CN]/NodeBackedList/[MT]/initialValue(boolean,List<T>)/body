{
  if (delegate == null) {
    if (write) {
      delegate=new ArrayList<T>();
      ScalarCollectionSchema.set(modelNode,delegate);
    }
 else {
      delegate=Collections.emptyList();
    }
  }
  return delegate;
}
