{
  T object;
  if (domainObject != null) {
    object=domainObject;
  }
 else {
    object=configureDomainObject();
  }
  try {
    generator.write(object,getOutputFile());
  }
  finally {
    domainObject=null;
  }
}
