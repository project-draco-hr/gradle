{
  if (dir == null) {
    if (prefix == null) {
      prefix=determinePrefix();
    }
    while (true) {
      dir=root.file(prefix,Integer.toString(RANDOM.nextInt(),36));
      if (dir.mkdirs()) {
        break;
      }
    }
  }
  return dir;
}
