{
  type=delegate.type;
  source=type.cast(Proxy.newProxyInstance(type.getClassLoader(),new Class<?>[]{type},new DelegatingBroadcastInvocationHandler(Proxy.getInvocationHandler(delegate.source))));
}
