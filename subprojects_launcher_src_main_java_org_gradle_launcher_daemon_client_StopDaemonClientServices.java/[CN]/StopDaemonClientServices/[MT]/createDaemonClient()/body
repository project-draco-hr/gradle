{
  ExplainingSpec<DaemonContext> matchAll=Specs.satisfyAll();
  return new DaemonClient(get(DaemonConnector.class),get(OutputEventListener.class),matchAll,getBuildStandardInput(),get(ExecutorFactory.class),get(IdGenerator.class));
}
