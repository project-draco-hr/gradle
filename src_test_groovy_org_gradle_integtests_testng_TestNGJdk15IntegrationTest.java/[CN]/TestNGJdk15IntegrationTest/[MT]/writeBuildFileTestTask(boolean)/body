{
  List<String> lines=new ArrayList<String>();
  lines.addAll(Arrays.asList("test {","   useTestNG().options.dumpCommand() "));
  if (!testReport) {
    lines.addAll(Arrays.asList("disableTestReport()"));
  }
  lines.addAll(Arrays.asList("}"));
  return lines;
}
