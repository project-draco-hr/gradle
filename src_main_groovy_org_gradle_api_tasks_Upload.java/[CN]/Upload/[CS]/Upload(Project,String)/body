{
  super(project,name);
  uploadResolvers=new ResolverContainer(new LocalReposCacheHandler(project.getDependencies().getBuildResolverDir()));
  doFirst(new TaskAction(){
    public void execute(    Task task){
      upload(task);
    }
  }
);
}
