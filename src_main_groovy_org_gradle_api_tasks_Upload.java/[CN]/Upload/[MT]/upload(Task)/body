{
  logger.info("Publishing configurations: " + configuration);
  configuration.publish(repositories.getResolvers(),new PublishInstruction(isUploadDescriptor(),isUploadDescriptor() ? getDescriptorDestination() : null));
}
