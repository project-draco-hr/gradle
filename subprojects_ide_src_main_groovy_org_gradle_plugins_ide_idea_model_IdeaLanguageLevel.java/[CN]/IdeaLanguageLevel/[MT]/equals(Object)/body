{
  if (this == o) {
    return true;
  }
  if (!getClass().equals(o.getClass())) {
    return false;
  }
  IdeaLanguageLevel that=(IdeaLanguageLevel)o;
  if (!level.equals(that.level)) {
    return false;
  }
  return true;
}
