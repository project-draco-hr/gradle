{
  if (original == null) {
    PrintStream originalStream=get();
    original=new PrintStreamDestination(originalStream);
  }
  outstr.flush();
  textOutput.flush();
  if (get() != outstr) {
    set(outstr);
  }
}
