{
  if (buildScriptClassName == null)   throw new NullPointerException();
  loadPropertiesIfNecessary();
  String path=properties.getProperty(buildScriptClassName);
  return path != null ? new File(path) : null;
}
