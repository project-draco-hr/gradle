{
  HashCode hashCode=ignoreHash ? IGNORED_HASH : calculateClassLoaderHash(classPath);
  ClassLoader classLoader=super.doCreateClassLoader(parent,classPath);
  hashCodes.put(classLoader,hashCode);
  return classLoader;
}
