{
  final String configurationName=configuration.getName();
  context.checking(new Expectations(){
{
      allowing(dependencyDescriptorFactoryStub).addDependencyDescriptor(with(equal(configurationName)),with(equal(parent)),with(sameInstance(dependency)));
    }
  }
);
}
