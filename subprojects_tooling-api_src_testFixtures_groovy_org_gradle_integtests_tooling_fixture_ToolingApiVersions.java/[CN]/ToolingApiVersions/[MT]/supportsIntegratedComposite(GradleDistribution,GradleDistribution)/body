{
  Spec<GradleVersion> gradleVersionSpec=GradleVersionSpec.toSpec(ToolingApiVersions.SUPPORTS_INTEGRATED_COMPOSITE);
  return gradleVersionSpec.isSatisfiedBy(version(toolingDistribution)) && gradleVersionSpec.isSatisfiedBy(version(targetDistribution));
}
