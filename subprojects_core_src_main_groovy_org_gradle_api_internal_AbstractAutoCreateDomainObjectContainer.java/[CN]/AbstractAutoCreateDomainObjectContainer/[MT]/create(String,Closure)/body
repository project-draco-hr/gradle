{
  if (findByName(name) != null) {
    throw new InvalidUserDataException(String.format("Cannot add %s '%s' as a %s with that name already exists.",getTypeDisplayName(),name,getTypeDisplayName()));
  }
  T object=doCreate(name);
  add(object);
  ConfigureUtil.configure(configureClosure,object);
  return object;
}
