{
  return ivyService.getModuleDescriptorConverter().convert(new HashMap<String,Boolean>(),configurationContainer,configurationSpec,dependencyContainer,dependencySpec,artifactContainer,artifactSpec);
}
