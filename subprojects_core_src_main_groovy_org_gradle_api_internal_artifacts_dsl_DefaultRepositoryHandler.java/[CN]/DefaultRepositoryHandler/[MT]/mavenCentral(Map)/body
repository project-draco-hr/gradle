{
  List<String> urls=createStringifiedListFromMapArg(args,"urls");
  return addLast(getResolverFactory().createMavenRepoResolver(getNameFromMap(args,DEFAULT_MAVEN_CENTRAL_REPO_NAME),MAVEN_CENTRAL_URL,urls == null ? new String[0] : urls.toArray(new String[urls.size()])));
}
