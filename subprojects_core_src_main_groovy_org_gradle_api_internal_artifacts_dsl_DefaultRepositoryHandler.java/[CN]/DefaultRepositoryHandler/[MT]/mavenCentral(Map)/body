{
  List<Object> urls=createListFromMapArg(args,"urls");
  return addLast(getResolverFactory().createMavenRepoResolver(getNameFromMap(args,DEFAULT_MAVEN_CENTRAL_REPO_NAME),MAVEN_CENTRAL_URL,urls.toArray()));
}
