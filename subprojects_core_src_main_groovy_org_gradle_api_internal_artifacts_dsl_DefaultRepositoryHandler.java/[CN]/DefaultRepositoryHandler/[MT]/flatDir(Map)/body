{
  Object[] rootDirPaths=getFlatDirRootDirs(args);
  FileSystemResolver resolver=getResolverFactory().createFlatDirResolver(getNameFromMap(args,HashUtil.createHash(GUtil.join(rootDirPaths,""))),rootDirPaths);
  return (FileSystemResolver)addLast(resolver);
}
