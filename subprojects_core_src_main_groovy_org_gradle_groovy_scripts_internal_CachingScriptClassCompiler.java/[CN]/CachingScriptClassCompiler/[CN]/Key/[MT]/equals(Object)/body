{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Key key=(Key)o;
  return baseClassName.equals(key.baseClassName) && classLoader.equals(key.classLoader) && className.equals(key.className)&& transformerId.equals(key.transformerId);
}
