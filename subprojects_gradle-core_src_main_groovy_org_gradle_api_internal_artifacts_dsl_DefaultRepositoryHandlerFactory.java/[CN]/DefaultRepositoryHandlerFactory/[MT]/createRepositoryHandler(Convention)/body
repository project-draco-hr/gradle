{
  DefaultRepositoryHandler repositoryHandler=classGenerator.newInstance(DefaultRepositoryHandler.class,repositoryFactory,classGenerator);
  ((IConventionAware)repositoryHandler).getConventionMapping().setConvention(convention);
  return repositoryHandler;
}
