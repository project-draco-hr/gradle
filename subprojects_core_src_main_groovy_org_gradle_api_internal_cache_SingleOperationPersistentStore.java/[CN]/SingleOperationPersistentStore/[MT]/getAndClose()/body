{
  try {
    return cacheAccess.useCache("storing " + cacheName,new Factory<V>(){
      public V create(){
        return cache.get(CACHE_KEY);
      }
    }
);
  }
  finally {
    cacheAccess.close();
  }
}
