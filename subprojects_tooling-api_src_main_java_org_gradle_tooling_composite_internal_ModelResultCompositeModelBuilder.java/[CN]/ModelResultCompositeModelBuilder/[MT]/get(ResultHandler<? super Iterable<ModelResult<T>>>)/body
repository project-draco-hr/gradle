{
  delegate.get(new ResultHandler<Set<T>>(){
    @Override public void onComplete(    Set<T> result){
      handler.onComplete(transform(result));
    }
    @Override public void onFailure(    GradleConnectionException failure){
      handler.onFailure(failure);
    }
  }
);
}
