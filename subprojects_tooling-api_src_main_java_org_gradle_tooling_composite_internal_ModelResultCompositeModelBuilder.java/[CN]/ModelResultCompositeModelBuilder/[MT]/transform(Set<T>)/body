{
  return CollectionUtils.collect(results,new Transformer<ModelResult<T>,T>(){
    @Override public ModelResult<T> transform(    T t){
      return new DefaultModelResult<T>(t,extractProjectIdentityHack(t));
    }
  }
);
}
