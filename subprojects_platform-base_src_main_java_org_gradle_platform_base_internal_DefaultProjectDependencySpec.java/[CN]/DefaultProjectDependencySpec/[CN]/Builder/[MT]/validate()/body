{
  if (projectPath == null && libraryName != null && libraryName.contains(":")) {
    throw new IllegalArgumentException(String.format("`%s' is not a valid library name. Did you mean to refer to a module instead?",libraryName));
  }
}
