{
  doWithClassPath(new ErroringAction<Strategy>(){
    @Override protected void doExecute(    Strategy strategy) throws Exception {
      strategy.prepare(leakingLoader,affectedLoaders);
    }
  }
);
}
