{
  List<DefaultGradleTask> out=new LinkedList<DefaultGradleTask>();
  for (  Task t : tasks) {
    out.add(new LaunchableGradleTask().setPath(t.getPath()).setName(t.getName()).setDisplayName(t.toString()).setDescription(t.getDescription()).setProject(owner));
  }
  return out;
}
