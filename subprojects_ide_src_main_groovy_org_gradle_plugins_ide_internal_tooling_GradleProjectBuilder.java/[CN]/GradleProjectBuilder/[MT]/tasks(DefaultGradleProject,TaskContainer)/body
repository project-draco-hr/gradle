{
  List<GradleTask> out=new LinkedList<GradleTask>();
  for (  Task t : tasks) {
    out.add(new DefaultGradleTask().setPath(t.getPath()).setName(t.getName()).setDescription(t.getDescription()).setProject(owner));
  }
  return out;
}
