{
  if (delegate == null) {
    delegate=new DefaultLibraryResolver(dependency).withFlavor(target.getFlavor()).withToolChain(target.getToolChain()).withPlatform(target.getTargetPlatform()).withBuildType(target.getBuildType()).resolve();
  }
  return delegate;
}
