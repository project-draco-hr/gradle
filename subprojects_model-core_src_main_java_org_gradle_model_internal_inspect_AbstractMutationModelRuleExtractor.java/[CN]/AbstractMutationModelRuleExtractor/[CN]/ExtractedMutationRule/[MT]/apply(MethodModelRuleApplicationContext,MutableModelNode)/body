{
  context.getRegistry().configure(mutationType,new MethodBackedModelAction<S>(context.invokerFor(ruleDefinition),ruleDefinition.getDescriptor(),ruleDefinition.getSubjectReference(),ruleDefinition.getTailReferences()),target.getPath());
}
