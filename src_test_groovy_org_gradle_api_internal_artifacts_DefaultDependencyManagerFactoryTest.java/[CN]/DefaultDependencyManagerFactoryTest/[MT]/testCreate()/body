{
  DefaultDependencyManager dependencyManager=(DefaultDependencyManager)new DefaultDependencyManagerFactory(settingsFinderMock,CacheUsage.ON).createDependencyManager(expectedProject,testGradleUserHome);
  assertTrue(expectedBuildResolverDir.isDirectory());
  checkCommon(expectedProject,dependencyManager);
}
