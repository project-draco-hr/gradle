{
  DefaultDependencyManager dependencyManager=(DefaultDependencyManager)new DefaultDependencyManagerFactory(settingsFinderMock,CacheUsage.REBUILD).createDependencyManager(expectedProject,testGradleUserHome);
  assertTrue(!expectedBuildResolverDir.isDirectory());
  checkCommon(expectedProject,dependencyManager);
}
