{
  for (  String testSrcDir : existingRelativePaths(getTestSrcDirs())) {
    classpathEntry(root,SRC,testSrcDir).addAttribute(OUTPUT,getProject().relativePath(getTestOutputDirectory()).toString());
  }
}
