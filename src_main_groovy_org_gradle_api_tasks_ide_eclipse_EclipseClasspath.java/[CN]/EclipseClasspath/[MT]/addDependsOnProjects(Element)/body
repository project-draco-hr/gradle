{
  for (  Project dependsOnProject : EclipseUtil.getDependsOnProjects(getProjectDependencies())) {
    classpathEntry(root,SRC,"/" + dependsOnProject.getName()).addAttribute(COMBINEACCESSRULES,"false");
  }
}
