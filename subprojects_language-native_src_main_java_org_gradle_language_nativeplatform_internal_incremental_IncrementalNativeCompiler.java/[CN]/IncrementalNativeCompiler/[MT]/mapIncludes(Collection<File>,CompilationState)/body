{
  return CollectionUtils.collectMapValues(files,new Transformer<SourceIncludes,File>(){
    @Override public SourceIncludes transform(    File file){
      return compilationState.getState(file).getSourceIncludes();
    }
  }
);
}
