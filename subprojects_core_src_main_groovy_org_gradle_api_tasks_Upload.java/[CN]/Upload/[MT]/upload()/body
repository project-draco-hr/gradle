{
  logger.info("Publishing configuration: " + configuration);
  ConfigurationInternal configurationInternal=(ConfigurationInternal)configuration;
  artifactPublisher.publish(configurationInternal.getModule(),configurationInternal,isUploadDescriptor() ? getDescriptorDestination() : null,null);
}
