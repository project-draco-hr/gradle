{
  logger.info("Publishing configuration: " + configuration);
  artifactPublisher.publish((ConfigurationInternal)configuration,isUploadDescriptor() ? getDescriptorDestination() : null,isUploadDescriptor() ? getDescriptorModification() : null);
}
