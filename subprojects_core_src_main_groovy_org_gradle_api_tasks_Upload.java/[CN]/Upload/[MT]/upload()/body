{
  logger.info("Publishing configuration: " + configuration);
  ConfigurationInternal configurationInternal=(ConfigurationInternal)configuration;
  artifactPublisher.publish(configurationInternal.getModule(),configuration.getHierarchy(),isUploadDescriptor() ? getDescriptorDestination() : null,null);
}
