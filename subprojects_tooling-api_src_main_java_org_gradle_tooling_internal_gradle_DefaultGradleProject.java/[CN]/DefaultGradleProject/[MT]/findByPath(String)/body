{
  if (path.equals(this.path)) {
    return this;
  }
  for (  GradleProject child : children) {
    GradleProject found=child.findByPath(path);
    if (found != null) {
      return found;
    }
  }
  return null;
}
