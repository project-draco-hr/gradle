{
  return String.format("Repos cached: %s, cache instances: %s, modules served from cache: %s (local %s), artifacts: %s",reposWrapped,cacheInstances,metadataCached,localMetadataCached,artifactsCached);
}
