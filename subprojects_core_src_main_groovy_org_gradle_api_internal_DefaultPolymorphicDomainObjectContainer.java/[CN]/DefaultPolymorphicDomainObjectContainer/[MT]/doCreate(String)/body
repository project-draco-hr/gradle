{
  if (defaultFactory == null) {
    throw new InvalidUserDataException(String.format("This container does not support " + "creating %s's without specifying a type.",getTypeDisplayName()));
  }
  return defaultFactory.create(name);
}
