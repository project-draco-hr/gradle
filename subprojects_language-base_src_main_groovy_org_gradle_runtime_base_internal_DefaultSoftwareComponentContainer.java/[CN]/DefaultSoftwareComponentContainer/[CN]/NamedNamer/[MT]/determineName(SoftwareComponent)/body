{
  if (component instanceof Named) {
    return ((Named)component).getName();
  }
  throw new IllegalArgumentException(String.format("Component %s cannot be added to %s as it is does not implement %s.",component,SoftwareComponentContainer.class.getSimpleName(),Named.class.getSimpleName()));
}
