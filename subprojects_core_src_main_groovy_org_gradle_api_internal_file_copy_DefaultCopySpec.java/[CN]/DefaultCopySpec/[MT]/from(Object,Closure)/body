{
  if (c == null) {
    from(sourcePath);
    return this;
  }
 else {
    DefaultCopySpec child=addChild();
    child.from(sourcePath);
    ConfigureUtil.configure(c,child);
    return child;
  }
}
