{
  if (buildAbility == null) {
    buildAbility=new CompositeBuildAbility(new ConfigurableBuildAbility(buildable),new ToolSearchBuildAbility(((JavaToolChainInternal)getToolChain()).select(getTargetPlatform())));
  }
  return buildAbility;
}
