{
  return ImmutableMap.<String,Collection<Configuration>>builder().put("plus",plus == null ? Collections.<Configuration>emptyList() : Collections.singletonList(plus)).put("minus",Collections.<Configuration>emptyList()).build();
}
