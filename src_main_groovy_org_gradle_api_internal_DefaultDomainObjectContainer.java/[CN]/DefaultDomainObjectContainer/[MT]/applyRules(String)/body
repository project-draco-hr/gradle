{
  if (name.equals(applyingRulesFor)) {
    return;
  }
  applyingRulesFor=name;
  try {
    for (    Rule rule : rules) {
      rule.apply(name);
    }
  }
  finally {
    applyingRulesFor=null;
  }
}
