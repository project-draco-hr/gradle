{
  DefaultBuildStartedProgressEvent startEvent=new DefaultBuildStartedProgressEvent(eventTracker.eventTime(),new DefaultBuildDescriptor(EventIdGenerator.generateId(gradle,"projectsLoaded"),"projects loading",EventIdGenerator.generateId(gradle)));
  DefaultBuildFinishedProgressEvent endEvent=new DefaultBuildFinishedProgressEvent(eventTracker.eventTime(),new DefaultBuildDescriptor(EventIdGenerator.generateId(gradle,"projectsLoaded"),"projects loading",EventIdGenerator.generateId(gradle)),new DefaultBuildSuccessResult(eventTracker.buildStartTime(),eventTracker.eventTime()));
  eventConsumer.dispatch(startEvent);
  eventConsumer.dispatch(endEvent);
}
