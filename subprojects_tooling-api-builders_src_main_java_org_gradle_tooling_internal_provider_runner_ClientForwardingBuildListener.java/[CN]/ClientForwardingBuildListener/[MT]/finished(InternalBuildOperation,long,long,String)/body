{
  DefaultBuildDescriptor descriptor=createDescriptor(source,eventType,eventType + " finished");
  DefaultBuildOperationFinishedProgressEvent finishEvent=new DefaultBuildOperationFinishedProgressEvent(endTime,descriptor,adaptResult(source.getPayload(),startTime,endTime));
  eventConsumer.dispatch(finishEvent);
}
