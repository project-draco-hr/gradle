{
  DefaultBuildDescriptor descriptor=createDescriptor(source,eventType,eventType + " finished");
  DefaultBuildFinishedProgressEvent finishEvent=new DefaultBuildFinishedProgressEvent(endTime,descriptor,adaptResult(source.getPayload(),startTime,endTime));
  eventConsumer.dispatch(finishEvent);
}
