{
  DefaultBuildDescriptor descriptor=createDescriptor(source,eventType,eventType + " started");
  DefaultBuildOperationStartedProgressEvent startEvent=new DefaultBuildOperationStartedProgressEvent(startTime,descriptor);
  eventConsumer.dispatch(startEvent);
}
