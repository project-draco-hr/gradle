{
  DefaultBuildDescriptor descriptor=createDescriptor(source,eventType,eventType + " finished");
  DefaultBuildFinishedProgressEvent finishEvent=new DefaultBuildFinishedProgressEvent(endTime,descriptor,adaptResult(source,startTime,endTime));
  eventConsumer.dispatch(finishEvent);
}
