{
  DefaultResolvedDependency parent=new DefaultResolvedDependency("someGroup",parentName,"someVersion","someConfiguration",Collections.<ResolvedArtifact>emptySet());
  resolvedDependency.getParents().add(parent);
  resolvedDependency.addParentSpecificArtifacts(parent,parentSpecificArtifacts);
  return parent;
}
