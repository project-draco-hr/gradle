{
  addWithId(pluginId);
  assertThat(getPluginContainer().hasPlugin(pluginId),equalTo(true));
  assertThat(getPluginContainer().hasPlugin(getPluginWithId().getClass()),equalTo(true));
  assertThat((TestPlugin1)getPluginContainer().findPlugin(pluginId),sameInstance(getPluginWithId()));
  assertThat((TestPlugin1)getPluginContainer().findPlugin(getPluginWithId().getClass()),sameInstance(getPluginWithId()));
}
