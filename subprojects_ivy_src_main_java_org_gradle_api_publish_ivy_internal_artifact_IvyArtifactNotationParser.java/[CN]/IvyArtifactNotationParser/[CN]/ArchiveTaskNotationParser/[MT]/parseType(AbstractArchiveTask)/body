{
  DefaultIvyArtifact ivyArtifact=instantiator.newInstance(DefaultIvyArtifact.class,archiveTask.getArchivePath(),archiveTask.getBaseName(),archiveTask.getExtension(),archiveTask.getExtension());
  ivyArtifact.builtBy(archiveTask);
  return ivyArtifact;
}
