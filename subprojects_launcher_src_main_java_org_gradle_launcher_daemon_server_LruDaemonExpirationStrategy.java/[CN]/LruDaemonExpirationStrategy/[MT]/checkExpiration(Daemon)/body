{
  DaemonRegistry registry=daemon.getDaemonRegistry();
  return registry.getAll().size() > 1 && isOldest(daemon.getDaemonContext(),registry) ? new DaemonExpirationResult(true,"This is the least recently used daemon") : new DaemonExpirationResult(false,null);
}
