{
  DaemonRegistry registry=daemon.getDaemonRegistry();
  return registry.getAll().size() > 1 && isOldest(daemon.getDaemonContext(),registry) ? new DaemonExpirationResult(true,false,false,"This is the least recently used daemon") : DaemonExpirationResult.DO_NOT_EXPIRE;
}
