{
  DaemonRegistry registry=daemon.getDaemonRegistry();
  return registry.getAll().size() > 1 && isOldest(daemon.getDaemonContext(),registry) ? new DaemonExpirationResult(QUIET_EXPIRE,"This is the least recently used daemon") : DaemonExpirationResult.NOT_TRIGGERED;
}
