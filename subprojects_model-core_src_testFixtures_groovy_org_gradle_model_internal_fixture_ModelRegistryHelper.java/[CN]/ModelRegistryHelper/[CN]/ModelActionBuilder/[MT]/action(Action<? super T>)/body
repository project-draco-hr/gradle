{
  return build(NO_REFS,new TriAction<MutableModelNode,T,Inputs>(){
    @Override public void execute(    MutableModelNode mutableModelNode,    T t,    Inputs inputs){
      action.execute(t);
    }
  }
);
}
